# レビューバックログ（今回対応しない指摘）
| 追記日 | ID | 重大度 | 場所 | 内容 | 起票状況 |
|---|---|---|---|---|---|
| 2026-08-28 | R1-L1 | Low | includes/Woo/Writer/CustomerWriter.php:75-79 | `write()`内`if(null===$user_id)`が`resolve_target()`導入後に到達不能なデッドコードになっている | 未起票 |
| 2026-08-28 | R1-L2 | Low | includes/Admin/RestController.php:78-96 | CSVストリーミングの`rest_pre_serve_request`自己解除フィルター配線は実HTTPサーブ経路でのみ発火し、`dispatch()`ベースのユニットテストでは配線自体をE2E確認できない（既存方針でintegration testに分離すべき範囲） | 未起票 |
| 2026-09-04 | f1-5-list-price/R1-X1 | High（対象外） | includes/Adapters/ColorMe/ColorMeAdapter.php:305,371,390 | `order_transformer()`の基盤取得（payments.json/deliveries.json）失敗が`transform_rows_flat()`の行レベルcatchに飲み込まれ、「1件目の受注の変換失敗」として毎回誤ログされページ全体が0件変換で完了しうる。`fetch_product_by_remote_id`で同種バグを修正した際に発見（R1参照）。別issue化を推奨 | 未起票 |
| 2026-09-04 | f1-5-list-price/R1-L1 | Low | includes/Adapters/ColorMe/Transform/ProductTransformer.php:127-134 | `round_off`（四捨五入と推測）の丸め挙動はswaggerに明記されておらず実機未検証（端数が出ない価格でしか実測していない）。F1-8実機E2Eで端数の出る価格の商品を使って確認すること | 未起票 |
| 2026-09-04 | f1-5-list-price/R1-L2 | Low | includes/Adapters/ColorMe/ColorMeAdapter.php:521-538 | `product_transformer()`はインスタンス単位でのみ`shop.json`をキャッシュし、プロセスを跨ぐ大規模カタログ同期では毎ページ再取得しうる（`order_transformer()`も同様の既存パターン）。`test_connection()`が`contract_plan`を`TokenStore`のextrasにキャッシュしている仕組みを税設定にも適用すれば削減可能 | 未起票 |
| 2026-09-06 | order-line-variation-resolution/R1-L1 | Low | includes/Woo/Support/ProductResolver.php:95-149 | `resolve_variation_by_options()`は受注明細1件ごとに`get_children()`+variationごとの`wc_get_product()`を都度実行し、`MappingRepository::find_many()`のようなバッチ先読みをしない（同一注文内の同一variable親への複数明細でも都度再取得）。実害は小さいが`VariationWriter`/`ProductWriter::resolve_refs()`のN+1回避方針とは非対称 | 未起票 |
| 2026-09-06 | order-line-variation-resolution/R1-L2 | Low | includes/Admin/RestController.php:190-207 | 新設`/settings/mappings/{platform}`ルートはGET/PUTとも`register_rest_route()`に`args`スキーマ（type検証）を定義していない。実害はPUT側の手動`is_array()`チェックで無いが、他ルート同様に自己文書化しておくとREST discoverability向上に資する | 未起票 |
| 2026-09-06 | f1-6-import-ui/R1-X1 | Medium（対象外） | includes/Sync/JobManager.php:133-146, includes/Admin/RestController.php:cancel_run() | `cancel_run()`はDB上のjob状態を直接`cancelled`に更新するのみで、Action Scheduler側で実行中の`process_job()`を中断しない。ページ処理の実行中に外部からcancelされた場合、`process_job()`はページ処理完了後に`complete_job_and_advance()`が無条件に`STATUS_COMPLETED`を書き込み、直前のcancelled状態を上書きする競合状態がありうる。F1-6 PR-BでCancelボタンを初めてUIに露出させたことで顕在化しやすくなった | 未起票 |
| 2026-09-06 | f1-6-import-ui/R1-L2 | Low | src/tabs/LogsTab.tsx:1272 | ページングの「Next」無効化判定`logs.length < 50`がバックエンド（`Sync\LogRepository::list()`の`$per_page`デフォルト値）とハードコードで結合しており、バックエンド側のデフォルトが変わると無言でページング判定が壊れる | 未起票 |
| 2026-09-07 | f1-6-import-ui/R2-X1 | High（対象外） | includes/Admin/RestController.php（`/runs`関連ルート全般） | `POST /runs`がバックエンド側でジョブを作成・実行開始した直後、その応答をブラウザが受け取れなかった場合（リロード・通信断等）、run_idがlocalStorageに保存されず以後発見不能になる。`JobManager::has_active_job_for_platform()`により同プラットフォームへの新規実行は409で拒否され続けるが、進捗確認・キャンセルの手段が無くなる。根本対応にはプラットフォーム単位でアクティブなrunを検索する新規RESTエンドポイントが必要（Codexレビュー指摘、PR #30） | 未起票 |
| 2026-09-07 | f1-6-import-ui/R3-X1 | Medium（対象外） | src/tabs/ImportTab.tsx（Preview resultsのCSVレポートUI）, includes/Sync/DryRunItemRepository.php（保持期間） | localStorageのrun_idには有効期限が無く、`cbjp_dry_run_items`の保持期間（`cbjp/dry_run_items/retention_days`フィルター、既定30日）を過ぎた古いdry-run結果を復元して開くと、行が既に削除済みでもCSVダウンロードボタンが表示され続け、クリックするとヘッダーのみの空CSVが返る（一見「警告0件で正常」に見えて紛らわしい）。フロントはジョブの作成日時を取得できず、保持日数もフィルターで変更されうるため、根本対応にはバックエンド側（タイムスタンプの公開や期限切れ判定の追加等）が必要（Codexレビュー指摘、PR #30） | 未起票 |
| 2026-09-11 | f1-7-tools/R1-L1 | Low | includes/Woo/Tools/SampleCleanup.php:owned_attachment_ids | 所有添付の判定を`_cbjp_platform`のみで行っている。`ProductWriter::set_images()`の所有権述語（`_cbjp_source_url`あり かつ `_cbjp_platform`一致）と揃えると防御が厚くなる（現状`_cbjp_platform`は`MediaImporter`経由の添付にしか付かないため実害なし） | 未起票 |
| 2026-09-11 | f1-7-tools/R1-L2 | Low | includes/Woo/Tools/LocalEntityLookup.php:existing_order_ids | 受注の実在確認・合計を`wc_get_order()`1件ずつで行う（HPOS/CPT両構成で確実に動く代わりにPro の大量受注では受注数分のクエリ）。`wc_get_orders(['id'=>[...]])`一括読込はHPOSでは`post__in`→`id` aliasで動くことを確認済みだがCPT構成が未検証のため見送り | 未起票 |
| 2026-09-11 | f1-7-tools/R1-L3 | Low | includes/Admin/RestController.php:run_sample_cleanup | アクティブジョブ有無のチェックと削除の間に排他が無く、バッチ実行中にimportが開始されるとmappings削除とupsertが競合しうる（`start_run()`側も楽観的）。プラットフォーム単位のロックはf1-6-import-ui/R1-X1（cancel競合）と合わせて検討 | 未起票 |
| 2026-09-11 | f1-7-tools/R1-L4 | Low | includes/Woo/Tools/SampleCleanup.php:remove_customer | `wp_delete_user()`をreassign無しで呼ぶため当該ユーザーの投稿があれば消える（importが作ったcustomerロールのアカウントは通常投稿を持たない）。マルチサイトでは当該サイトからの除外のみ | 未起票 |
| 2026-09-11 | f1-7-tools/R1-L5 | Low | includes/Woo/Tools/MappingRebuilder.php:run | `_cbjp_remote_id`が重複する実体（WooCommerce標準の「複製」はカスタムメタごと複製する）があるとID昇順走査の後勝ちでmappingが複製側を指し、以後のimportが複製を更新する。衝突の検出・ログが無い。同一remote_idを2件以上検出したらupsertせず警告ログ／conflict件数を返す改善余地 | 未起票 |
| 2026-09-11 | f1-7-tools/R1-L6 | Low | includes/Sync/Importer.php:process_items（remote_amount） | `Money::to_minor_units(...) ?? 0`は解析できない金額を無言で0として加算する（issue #14の「復元不能と0円の混同」と同型）。ただし`OrderWriter::validate_totals()`が非数値のtotalを持つ受注を`ORDER_TOTALS_INVALID`で丸ごと見送るためWoo側にも存在せず、レポート上は`fewer`として可視化される。別カウンタ（例`remote_amount_unknown`）で「金額突合不可」を出す改善余地 | 未起票 |
| 2026-09-11 | f1-7-tools/R1-X1（対応済み） | Medium（v2.0で顕在化） | includes/Woo/Writer/CustomerWriter.php:write（`_cbjp_platform`の上書き） | email突合で採用した既存アカウントにも`_cbjp_platform`を上書きするため、複数プラットフォーム構成では「Aが作成（marker あり）→Bが採用（platform=B）」となった顧客をBのクリーンアップが削除しうる。v1.0（単一プラットフォーム）では発生しない。B4-4/B4-5（BASE）着手時に、マーカーにplatformを持たせる等で対処 | **対応済み**（PR #34 G1-4: マーカー値を作成プラットフォームIDにし、採用側からは削除しない） |
| 2026-09-11 | f1-7-tools/R2-2 | Low | includes/Woo/Tools/SampleCleanup.php:deletable_attachment_ids | `MediaImporter`は同一`_cbjp_source_url`の添付を商品をまたいで再利用するため、`post_parent`（最初に取り込んだ商品）が消えると、別の生存商品（mapping消失・複製等）がギャラリー等で参照していても孤児と判定して削除する。`_thumbnail_id`/`_product_image_gallery`の参照確認を追加する改善余地 | 未起票 |
| 2026-09-11 | f1-7-tools/R2-3 | Low | includes/Woo/Tools/SampleCleanup.php:deletable_attachment_ids | 添付候補を毎バッチ全件取得・全親をプリロードするためO(N²/budget)。候補に上限を付けると削除不能な候補が窓を埋めて進まなくなるため全件判定にしている。`post_parent__in`等で候補を絞る改善余地（無料版はサンプル上限で実害薄） | 未起票 |
| 2026-09-11 | f1-7-tools/G1-6 | Medium（保留・要判断） | includes/Admin/RestController.php:run_sample_cleanup / includes/Sync/JobManager.php:start_run | クリーンアップ（複数バッチ）とimportの開始（`start_run()`）の間に共有ロックが無く、バッチ間に別タブでimportが開始されると削除と再作成が競合しうる（Codex指摘。R1-L3の再掲）。プラットフォーム単位のロック（transient等）を`start_run()`と共有する設計変更が必要で、cancel競合（f1-6-import-ui/R1-X1）と合わせて別issueで扱う | 未起票 |
| 2026-09-11 | f1-7-tools/G1-13 | Low | includes/Sync/MappingRepository.php:local_ids | Pro規模ではエンティティ毎の全local_idを1配列に載せる（int配列のため10万件でも数MB）。`LocalEntityLookup`はクエリを200件ずつに分けるが、ID列挙自体のページングはしていない。R1-L2と合わせてPro向けの最適化候補（Codex指摘） |未起票 |
| 2026-09-12 | fix-15-coupon/R1-L1 | Low | includes/Adapters/ColorMe/Transform/CouponTransformer.php:transform（`null` を返す8つの経路） | 変換層が `null` を返して除外した行は `ColorMeAdapter::transform_rows()` に無言で捨てられ（例外時のみログ）、dry-run レポート・結果レポートに一切現れない。店舗オーナーは「このクーポンが移行されなかった理由」（商品グループ制限・`status=unavailable`・開始日が未来・`ends_at` 欠損・未知の `coupon_type` 等）を知る手段が無い。issue #15 では制限の判定だけを `CanonicalCoupon::$has_unsupported_restrictions` に正規化したが、ColorMe は従来どおり変換層で除外する方針を維持したため本件は未解決のまま。全除外理由を一貫してレポートへ載せるには変換層から「除外された行とその理由」を返す仕組み（`Page` の拡張等）が必要で、他エンティティにも波及するため別タスク | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R1-L1（対応済み） | Low | includes/Admin/RestController.php:47-52（`SETTINGS_MAP_KEYS`のdocblock） | 「`Woo\Support\MethodMap`が読む…トップレベルキー」とあるが`category_map`は現状読み手が無い（E2-2/E2-3で消費予定）。docblockが実態と食い違う | **対応済み**（E2-2 PR-A: `MethodMap::mapped_asp_category_id()`を追加し`Woo\Reader\ProductReader`が消費するようになったため実態と一致） |
| 2026-09-13 | e2-1-mapping-ui/R1-L2 | Low | includes/Woo/Support/MappingCandidates.php:categories() / includes/Adapters/ColorMe/ColorMeAdapter.php:mapping_candidates() | Woo側・ASP側とも候補一覧が完全にフラットで親子階層情報（`WP_Term::$parent`/`CanonicalCategory::$parent_id`）を捨てている。親違いで同名の小カテゴリーがマッピングUIのドロップダウン上で区別できず誤マッピングが無警告で成立しうる | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R1-L3 | Low | includes/Woo/Support/MappingCandidates.php:categories() | `get_terms()`に件数上限が無く、カテゴリー数千件規模の店舗でGETペイロード・`<select>`のDOMが青天井に膨らむ（無料版はサンプル上限があるが実店舗規模では実害あり） | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R1-L4 | Low | includes/Woo/Support/MappingCandidates.php:shipping_methods() | `WC_Shipping_Zones::get_zones()`は`enabled_only=false`で呼ばれるため無効化された配送方法インスタンスも候補に出る。`MethodMap::shipping_method_instance_exists()`も有効/無効を見ないため、無効な配送方法が受注に書き込まれうる（対象外指摘R1-X3と対） | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R1-L5 | Low | src/tabs/ExportTab.tsx:MappingSection | 行内`SelectControl`に`label`/`aria-label`が無く、テーブルに`<thead>`/`<th>`も無いためスクリーンリーダーで名前の無いコントロールになる | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R1-L6 | Low | src/tabs/ExportTab.tsx:connectedPlatforms | `connected`単独で接続判定しており`needs_reconnect=true`（要再接続）を「未接続」と区別しない。`ImportTab.tsx`の既存パターンを踏襲したもので新規リグレッションではないが、Export/Import両タブでまとめて直すのが望ましい（CLAUDE.md「`connected`だけを見ると要再接続を未接続と区別できない」に該当） | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R1-L7 | Low | includes/Adapters/ColorMe/ColorMeAdapter.php:status_candidates() | `OrderTransformer::status()`が返しうる4値のcanonicalステータスをハードコードで複製しており、両者を結びつけるテスト・定数が無い。`status()`に5つ目の分岐が入るとマッピングUIから該当行が無言で欠落する | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R1-L8 | Low | includes/Adapters/ColorMe/ColorMeAdapter.php:mapping_candidates()経由のfetch_categories() | `CategoryTransformer::is_visible()`により`hidden`/`members_only`のColorMeカテゴリーがマッピング候補からも除外される（インポート時は正しい安全側判断だが、エクスポート先としては非公開カテゴリーへ入れたい要件がありうる） | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R1-L9 | Low | tests/unit/Adapters/ColorMe/ColorMeAdapterTest.php:test_mapping_candidates_returns_category_payment_shipping_and_status | カテゴリーフィクスチャが小カテゴリーID形式（`{id_big}-{id_small}`）や`name`のアサートを含まない | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R1-L10 | Low | src/tabs/ExportTab.tsx（プラットフォーム切替useEffect） | 未保存の編集内容を確認なしに破棄する（v1.0はColorMe単独でプラットフォームセレクタ自体が出ないため実害は将来のBASE/MakeShop対応時） | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R1-X1（対象外） | Low | includes/Woo/Support/MethodMap.php:14-16 | クラスdocblockが「`/settings/mappings/{platform}` REST が書く。現状は未実装のため常に空配列」のままで、F1-5でRESTが、E2-1でUIが実装済みという現状と食い違う | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R1-X2（対象外） | Low | includes/Adapters/AdapterRegistry.php | `get()`/`has()`がフィルター戻り値の各要素を`instanceof PlatformAdapter`で検証していない。`connection_fields()`等と同型の信頼境界の穴（アーキテクチャ原則8） | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R1-X3（対象外） | Low | includes/Woo/Support/MethodMap.php:56-58,99-105（payment_gateway_exists()/shipping_method_instance_exists()） | 「登録されているか」のみを見て有効/無効を判定しない。無効化されたゲートウェイ/配送方法が受注に書き込まれうる（e2-1-mapping-ui/R1-L4と対） | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R2-L1（対応済み） | Low | includes/Woo/Writer/OrderWriter.php:327 / includes/Woo/Support/MappingCandidates.php:130 | R1-2（`checkout-draft`のマッピング候補除外）はUI経由の候補一覧のみの防御で、消費側の`apply_status()`は`wc_get_order_statuses()`にあるキーを無条件に受理する。REST直PUTで`status_map`に`checkout-draft`を保存すれば、日次cronによる受注完全削除の経路自体は残る（UI経路は塞がれたため実害の蓋然性は低い） | **対応済み**（PR #39 G1-1: `MappingCandidates::is_disallowed_order_status()`を新設し`OrderWriter::apply_status()`でも同じ値をフェイルクローズするよう修正。Copilotのgate G3指摘で本行の記載が古いまま（`checkout-draft`除外実装済みなのに未対応のように見える）と指摘され訂正） |
| 2026-09-13 | e2-1-mapping-ui/R2-L2 | Low | src/tabs/ExportTab.tsx:99-104 / includes/Admin/RestController.php:537-561 | R1-6「失敗時は空配列」とR1-4「候補に無い値は(no longer available)表示」が重なると、ASP側候補取得が一時的に失敗しただけでカテゴリーマッピングの既存の正常な値が全行「(no longer available)」と表示されうる（保存しなければデータ損失は無い） | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R2-L3 | Low | src/tabs/ExportTab.tsx:89, 242-259 | R1-4はtarget側の孤立値のみ扱う。source側の孤立エントリ（廃止されたASP決済方法・削除されたWooカテゴリー等）は行が描画されないまま`edited`に残り保存で再永続化される（実害はほぼ無く不可視のゴミデータに留まる） | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/R2-L4 | Low | src/tabs/ExportTab.tsx:339-344 | 冒頭文言がカテゴリーマッピングの説明から始まるが、当該セクションは`can_create_category===false`のときしか描画されない。v1.0では常に表示されるため実害は無いが、将来`can_create_category=true`のASP追加時に説明だけが残る | 未起票 |
| 2026-09-13 | e2-1-mapping-ui/G3-3 | Low | src/tabs/ExportTab.tsx:94（`map[source.id]`）, 242-259（`updateMap()`の`next[sourceId]=...`） | マッピングの`map`/`edited`はプレーンオブジェクトのため、外部アダプタ（Pro拡張等）が`__proto__`/`constructor`/`toString`等をcandidate idとして返すと、読み取りはObject.prototypeの継承値を返し、書込みは`__proto__`のセッターに吸収されてエントリが作成されない（値は常に文字列のため`__proto__`セッターは非オブジェクト代入を無視し、プロトタイプ汚染そのものには至らない＝実害は「その行が保存されない」程度）。ColorMe（現状唯一のアダプタ）のidは常に数値文字列のため現時点の実害はほぼ無い。修正するには`Object.create(null)`ベースへの切替＋spread構文の置き換え（spreadは常に標準プロトタイプのオブジェクトを生成するため、コピー箇所すべてで`Object.assign(Object.create(null), ...)`に統一する必要があり修正範囲がやや広い）が必要 | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R1-M5 | Medium（保留） | includes/Woo/Reader/ProductReader.php:query（`type`フィルタ） | grouped/external等（simple/variable以外）の商品タイプはエクスポート対象から無警告で除外される（`ProductReader::query()`の`type`フィルタ）。dry-run結果・実行結果のどちらにも「対象外だった」件数が一切現れないため、店舗オーナーは一部商品がエクスポートされないことに気付けない | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R1-M8 | Medium（保留） | includes/Sync/Exporter.php（実行時は`cbjp_dry_run_items`を書かない既存方針） | 実行(非dry-run)のexportで全アイテムがskipped/warnedになっても（例: PR-A時点のColorMe`push_*`未実装）ジョブは`STATUS_COMPLETED`のまま終わり、個別警告はどこにも永続化されない（`Importer`と同じ既存方針を踏襲）。`totals_json`の集計（`created+updated===0 && warned>0`）からE2-4のUIが警告バナーを出す等の対応が必要 | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R1-L3 | Low | includes/Woo/Reader/ProductReader.php:variation_axis_attributes | バリエーション軸の並びを`WC_Product::get_attributes()`の格納順（`_product_attributes`postmeta順）に依存しており、`WC_Product_Attribute::get_position()`でソートしていない。他プラグイン・手動編集で属性順が入れ替わっている場合に軸1/軸2の対応がずれうる | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R1-L6 | Low | includes/Woo/Reader/ProductReader.php:variation_attribute_value | 「Any（任意）」ワイルドカードバリエーション（Wooが値を空文字列で保存する）が`option1_value=null`に変換され、警告も付かない。エクスポート先ASPが「任意」概念を持たない場合、無意味な組み合わせのバリエーションとして送られる | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R1-L7 | Low | includes/Woo/Reader/ProductReader.php:query（`$result->total`） | `wc_get_products(['paginate'=>true])`が返す`total`は`found_posts`（クエリ一致件数）で、実際に返る`products`は復元失敗分を`array_filter`で除いた後の件数と食い違いうる（CLAUDE.mdの`Page::$total`規約＝1:1対応を保証できない場合はnullを返す、に本来該当する）。復元失敗は稀なケースのため実害は小さい | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R1-L8 | Low | includes/Woo/Reader/ProductReader.php:images | メイン画像がギャラリーにも含まれる場合、重複してエクスポートされる（ColorMeの`mobile:true`重複画像とは逆方向の類似問題。CLAUDE.md参照） | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R1-L11 | Low | includes/Sync/Exporter.php:dry_run_row（`local:{local_id}`プレースホルダ） | `cbjp_dry_run_items.remote_id`のプレースホルダ`local:{id}`は、理論上ASP側の実remote_idと文字列として衝突しうる（`UNIQUE(job_id, entity, remote_id)`のため衝突時はCSV上の別行を上書きする）。ColorMeのremote_idは常に数値文字列のため実害は無いが、コロンを含むremote_idを返すASPが将来追加された場合に注意が必要 | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R1-L12 | Low | includes/Sync/ExportSampleSelector.php:top_up_products/top_up_customers | `SampleSelector::top_up_with_first_page()`は補完取得の失敗を`Logger::warning()`で記録するが、`ExportSampleSelector`にはLoggerが無く失敗を記録しない（Woo側の`wc_get_products()`/`get_users()`は例外を投げないため実害は小さいが非対称） | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R1-L14 | Low | includes/Woo/Export/AdapterPlatformWriter.php / includes/Woo/WooReaderRepository.php | 未知のentityに対する挙動が非対称: `AdapterPlatformWriter`はskipped（`WooRepository::write()`と同じ防御方針）、`WooReaderRepository`は`RuntimeException`（実装バグとして早期発見する方針）。両方とも意図的だが、方針の使い分けをコードコメント以外に文書化していない | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R1-L15 | Low | includes/Sync/Exporter.php → includes/Sync/MappingRepository.php:upsert（`remote_id varchar(191)`） | `PushResult::$remote_id`が191文字を超える場合、`$wpdb->query()`は警告なく失敗し`cbjp_mappings`行が書き込まれない（`totals['created']`は加算済みのため次回再実行で再作成される）。`cbjp/adapters/register`の信頼境界（アーキテクチャ原則8）上の防御漏れ | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R2-M-checksum-shared-row | Medium（保留・要判断） | includes/Sync/Exporter.php（`cbjp_mappings`の行共有） | R1-H1の修正（名前空間混ぜ込みハッシュ）は偽陽性（誤って一致とみなす）は防ぐが、import/exportの両方向が同じ行を触った実体は次の同期で必ず1回余分な再同期（Woo側の手動編集がimportで上書きされうる）が発生する構造は残る。真に無くすには`cbjp_mappings`への方向別checksum列追加（スキーマ変更）が必要。無料版のサンプル規模（最大50件）では実害小と判断しPR-Aでは対応しない。詳細はdocs/03-design-decisions.md §10.2 | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R2-M3-price-fallback | Medium（保留） | includes/Woo/Reader/ProductReader.php:price_fields_for_simple/price_fields_for_variable | 価格を復元できない商品（単純商品の価格未設定・variable商品の可視バリエーション0件）は警告付きだが`price='0'`のまま`ReadItem`として生成され続ける（在庫はフェイルクローズで0にする一方、価格はフェイルオープンで0円のまま実行時にpushされうる）。E2-3で実push可能になった時点で、無料商品としてASP側に作成されるリスクがある。`variants()`が同条件で行自体をスキップする（フェイルクローズ）のと非対称なため、商品レベルでも「読出自体をスキップ（`ReadItem`を生成しない）」方式への統一を検討すること | 未起票 |
| 2026-09-14 | e2-2-exporter-core/R2-L-sample-cleanup-preview | Low | includes/Woo/Tools/SampleCleanup.php:preview（`sample_selected`） | `preview()`が返す`sample_selected`はimport用サンプル（`SampleSelector`）のみを見ており、exportのみサンプル選定済みで削除対象の実体が無いユーザーには表示が不整合になりうる（`ExportSampleSelector`のサンプル存在も反映すべき） | 未起票 |
| 2026-09-14 | e2-2-exporter-core/G2-M-pro-tier-pagination | Medium（保留） | includes/Woo/Reader/ProductReader.php:57（`query()`のoffsetページング） | Pro版のフルカタログエクスポートでは各ページがAction Schedulerの別アクションとして実行されるため、既処理ページの商品が次ページ取得までに削除・除外条件変更されるとoffsetベースのページングでは後続ページが1件恒久的にスキップされうる。無料版（サンプル最大50件・単一ページ相当）はこの経路に到達しない。Pro版実装時にID起点のkeysetページングへの変更を検討すること | 未起票 |
| 2026-09-14 | e2-2-exporter-core/G2-M-ratelimit-partial-totals | Medium（保留） | includes/Sync/Exporter.php:179（`RateLimitExhaustedException`再スロー） | G1-1の修正（レート制限例外の再スロー）は同一ページ内で先行アイテムが既に書込・mapping upsert済みの状態でページ処理を中断する。再開後は先行アイテムがchecksum一致でskippedと再カウントされ、最終的なcreated/updated件数が実際より過小報告されうる（データの不整合ではなくtotalsの正確性のみの問題）。正確な部分totals報告にはページ処理を中断せず部分進捗をJobManagerへ伝播する設計変更が要る | 未起票 |
| 2026-09-14 | e2-2-exporter-core/G2-L-variation-read-n-plus-1 | Low | includes/Woo/Reader/ProductReader.php:288（`variants()`ループ内`wc_get_product()`） | バリエーション毎のマッピングはバッチ先読みしているが、`wc_get_product()`自体はループ内で1件ずつ呼ばれる（N+1）。`order-line-variation-resolution/R1-L1`と同種・同程度（実害は小さい）の指摘として同基準で保留する | 未起票 |
| 2026-09-14 | e2-2-exporter-core/G3-M-tax-basis-conversion | Medium（保留・一部対応） | includes/Woo/Reader/ProductReader.php:price_fields_for_simple/price_fields_for_variable | `woocommerce_prices_include_tax=no`の店舗では`get_regular_price()`が税抜金額を返すが、canonical price契約は税込前提。G3対応では`ProductWriter`（インポート方向）と対称の`PRICES_INCLUDE_TAX_DISABLED`警告のみ追加し、実際の税額換算（税区分×税率の適用・端数処理）は行っていない。インポート方向も同じ制約（警告のみ）のため対称ではあるが、真の解決には整数演算による税換算の設計（CLAUDE.md参照）が要る | 未起票 |
| 2026-09-14 | e2-2-exporter-core/G3-L-sampleselector-topup-gate-preexisting | Low（対象外・要横展開） | includes/Sync/SampleSelector.php:92（`if ($used_fallback)`によるtop_up_with_first_page()の呼び出しガード） | G3で`ExportSampleSelector`（本PR差分）に見つかった同一バグ（受注件数だけで判定する`$used_fallback`では、受注は上限件数あっても明細から抽出したエンティティが0件のケースを補完できない）が、差分範囲外の既存`SampleSelector`（import方向、E2-2より前にマージ済み）にも同じ構造で存在する。`top_up_with_first_page()`自身に`count($existing) >= $target`の内部ガードがあるため、外側の`if ($used_fallback)`を外す同じ修正が有効なはず。別PRで対応すること | 未起票 |
| 2026-09-14 | e2-2-exporter-pr-b/R1-L1 | Low | includes/Woo/Reader/CustomerReader.php:55, includes/Woo/Reader/StockReader.php:82 | `ReadPage::$total`が無料版サンプル（`only_local_ids`指定）時に常に`null`になる（`ProductReader`/`OrderReader`は同モードでも`count($items)`相当の数値を返す）。Export UI（E2-4）の進捗バーがcustomer/stockエンティティだけ分母を持てない可能性がある | 未起票 |
| 2026-09-14 | e2-2-exporter-pr-b/R1-L2 | Low | includes/Woo/Reader/StockReader.php:items_for_variable | 親商品は解決済みでも公開バリエーションが0件のvariable商品は、`stock`エンティティ側では警告なしで空配列を返す（`ALL_VARIATIONS_EXCLUDED`は`product`エンティティ側でのみ付く）。`product`エンティティを同一run内で選択していない場合、店舗オーナーは在庫が0件exportされた理由に気付けない | 未起票 |
| 2026-09-14 | e2-2-exporter-pr-b/R1-L3 | Low | includes/Woo/Reader/CouponReader.php:37 | `post_status=>'publish'`はdraft状態のクーポン（`WC_Coupon`の2回目`save()`が`post_status`を`null`扱いにして`draft`へ落ちる罠等。CLAUDE.md参照）を無警告で除外する。フェイルクローズ自体は正しいが、dry-runレポートにも一切現れないため店舗オーナーが「クーポンが1件も対象にならない」理由に気付けない | 未起票 |
| 2026-09-14 | e2-2-exporter-pr-b/R1-L4 | Low | includes/Canonical/CanonicalCustomer.php:22 | `$address`のdocblockがimport方向（ColorMeのpref_id/address1スキーム）のみを想定しており、export方向（`Woo\Reader\CustomerReader`/`OrderReader`が積むWooネイティブのpostcode/state/address_1スキーム）が未記載。方向によってキー体系が異なることをdocblockに明記すべき | 未起票 |
| 2026-09-14 | e2-2-exporter-pr-b/R1-L5 | Low | includes/Sync/ExportSampleSelector.php:86-91 / includes/Woo/Reader/CustomerReader.php:28 | `ExportSampleSelector`は`$order->get_customer_id()`をロールチェック無しでサンプルへ加えるが、`CustomerReader::query()`は`role=>'customer'`で絞り込む。注文の購入者がadmin/shop_manager等customer以外のロールだった場合、そのIDはサンプルの`customer_ids`に入るがReaderからは永久に返らず、紐づく注文は`ORDER_CUSTOMER_NOT_EXPORTED`を出し続ける（checksumが常に未キャッシュのまま再試行され続けるだけで実害は限定的） | 未起票 |
| 2026-09-14 | e2-2-exporter-pr-b/R1-L6 | Low | includes/Sync/JobManager.php:start_run | ColorMeは`can_create_coupon=false`のため、`start_run(TYPE_EXPORT, 'colorme', ['coupon'])`のように対応capabilityがfalseのエンティティのみを要求すると`RuntimeException('No supported entities to run.')`が投げられる。E2-4のExport UIはcapabilityでゲートされたエンティティをそもそも選択肢に出さない設計にする必要がある | 未起票 |
| 2026-09-14 | e2-2-exporter-pr-b/G1codex-M-sale-deliveries | Medium（保留・要判断） | includes/Woo/Reader/OrderReader.php:extras | ColorMeの複数配送先注文をインポートした場合、`Woo\Writer\OrderWriter`が`_cbjp_sale_deliveries`メタへ完全な配送先配列を保存済みだが、`OrderReader::extras()`はこれを読み戻さず新規構築した`customer_snapshot`/`paid`のみで置き換える。Wooの受注住所は代表（先頭）配送先1件分しか保持しないため、`push_order()`（E2-3）実装時に残りの配送先・配送指示を復元できない（Codex指摘, PR #41 #7）。アーキテクチャ原則1（プラットフォーム固有コードをアダプタ外に書かない）に照らすと、Woo Reader層が`_cbjp_sale_deliveries`というColorMe固有のextrasキー名をハードコードすべきかは設計判断が必要。E2-3で`push_order()`を実装する際、ColorMeアダプタ側で`_cbjp_sale_deliveries`メタを直接読む（Reader層を経由しない）設計も選択肢に入れて検討すること | 未起票 |
| 2026-09-14 | e2-2-exporter-pr-b/G1codex-M-customer-extras | Medium（保留・要判断） | includes/Woo/Reader/CustomerReader.php:to_read_item | ColorMeからインポートした顧客は`fax`/`tel_mobile`/`sex`/`points`/自由記入回答/会員種別等を`_cbjp_*`メタへ保存済みだが、`CustomerReader`は常に`extras=[]`を返す。ColorMe→Woo→ColorMeの往復で、`push_customer()`（E2-3）に渡せる情報がなくこれらの値が失われる（Codex指摘, PR #41 #8）。`G1codex-M-sale-deliveries`と同じ設計判断（Reader層でColorMe固有extrasキーを組み立てるか、E2-3のColorMeアダプタ側で直接`_cbjp_*`メタを読むか）が必要なため合わせて検討すること | 未起票 |
| 2026-09-14 | e2-2-exporter-pr-b/G1codex-M-coupon-cursor-stability | Medium（保留） | includes/Woo/Reader/CouponReader.php:query（`paged`によるoffsetページング） | `query()`は`'date'=>'DESC','ID'=>'DESC'`のoffsetベースページングを使うため、複数ページにまたがるクーポンexport中に新規クーポンが作成される・既存クーポンが削除/非公開化されると、offsetがずれて後続ページで1件重複処理または欠落しうる（Codex指摘, PR #41 #9）。`e2-2-exporter-core/G2-M-pro-tier-pagination`（`ProductReader`のoffsetページングに対する同種の指摘）と同じ判断を適用し、無料版の小規模ページング（`LimitPolicy::DEFAULT_LIMITS['coupon']=10`程度）では実害小として保留する。keysetページング（`(post_date, ID)`起点）への変更はPro版のフルカタログexport実装時にまとめて検討すること | 未起票 |
| 2026-09-15 | e2-3-push-product/R1-L1 | Low | includes/Adapters/ColorMe/Transform/ProductTransformer.php:to_push_amount | `'reduced-rate'`以外の税区分（`zero-rate`・店舗定義クラス）を無条件に標準税率へフォールバックする。読出方向`list_price_including_tax()`が`tax_reduced`欠損をnullで諦める（issue #24）のと非対称。最終税込額は往復で保存されるため実害は税区分表示のみ | 未起票 |
| 2026-09-15 | e2-3-push-product/R1-L2 | Low | includes/Adapters/ColorMe/ColorMeAdapter.php:push_product | セール終了後の更新で`price`（定価）を送らないため、ColorMe側に古い定価が残り「値引き中」表示が続きうる | 未起票 |
| 2026-09-15 | e2-3-push-product/R1-L3 | Low | includes/Adapters/ColorMe/ColorMeAdapter.php:push_variant_details | 一部variantだけ`stocks`を送ると、swagger記載どおり他variantの在庫が0に落ちる可能性がある（管理ON/OFF混在のvariable商品で無警告の在庫切れ） | 未起票 |
| 2026-09-15 | e2-3-push-product/R1-L4 | Low | includes/Adapters/ColorMe/ColorMeAdapter.php:push_images | 更新時も毎回全画像を再アップロードし、`GET /products/{id}/images`（swagger上存在）で既存を確認しない。5MB/MIMEの事前検証も無く、`POST /images`独自のレート制限（10秒5リクエスト、swagger記載）も`RateLimiter`でモデル化されていない | 未起票 |
| 2026-09-15 | e2-3-push-product/R1-L5 | Low | includes/Adapters/ColorMe/Transform/ProductTransformer.php:push_product | `to_create_payload()`を新規作成パスで2回組み立てている（副作用は無いが冗長） | 未起票 |
| 2026-09-15 | e2-3-push-product/R1-out-of-scope-price-fallback（**対応済み**） | High（対応済み） | includes/Woo/WarningCode.php:indicates_export_blocking | `e2-2-exporter-core/R2-M3-price-fallback`（本ファイル上部）が「E2-3で実push可能になった時点で対処を検討」と申し送っていた箇所。R1では対象外としたが、G1（PR #43ゲートラウンド）で`PRODUCT_PRICE_INVALID`を`indicates_export_blocking()`へ追加したため、価格復元不能な商品はpush自体がスキップされるようになり解消した（G2レビューでCopilotが本エントリの記述が実態と食い違うと指摘、G1時点で既に対応済みだったため本行の状態のみ更新）。`ProductReader`側の`'0'`フォールバック自体（`e2-2-exporter-core/R2-M3-price-fallback`）は引き続き未対応 | 対応済み |
| 2026-09-15 | e2-3-push-product/R2-L1 | Low | includes/Adapters/ColorMe/ColorMeAdapter.php:push_product | 価格解決不可（`PRODUCT_DETAILS_PUSH_INCOMPLETE`）と追いPUTのretryable失敗が重なると、同一の`PRODUCT_DETAILS_PUSH_INCOMPLETE`が2回積まれうる（`indicates_unresolved_reference()`の判定には影響しないが結果表示が冗長） | 未起票 |
| 2026-09-15 | e2-3-push-product/R2-L2 | Low | includes/Sync/Exporter.php:variant mapping書き戻し | バリエーションごとに`find_remote_id()`を個別呼び出ししており、親商品側の`find_many_by_local_ids()`のような一括プリロードをしない（N+1。実害は無料版の規模では小さい） | 未起票 |
| 2026-09-15 | e2-3-push-product/R2-L3 | Low | includes/Adapters/ColorMe/ColorMeAdapter.php:ensure_option_values | 既存オプションに`id`が無い（スキーマ破損）ケースを`terminal`（retry対象外）に分類している。実際にはAPI側の一時的な応答異常の可能性もあり、`retryable`寄りの分類の方が安全な可能性がある | 未起票 |
| 2026-09-15 | e2-3-push-product/G1-out-of-scope-prices-include-tax | High（対象外・要判断） | includes/Woo/Reader/ProductReader.php / includes/Adapters/ColorMe/Transform/ProductTransformer.php:to_push_amount | `woocommerce_prices_include_tax=no`の店舗（フレッシュなWP/WC環境でも既定でこの状態。実測: `JobManagerExportTest`が2件失敗した）では`ProductReader`が税抜金額を`PRICES_INCLUDE_TAX_DISABLED`警告付きで返すが、`to_push_amount()`は常に税込前提で換算するため実売価格を誤る（Codex指摘）。export blockingにすると無料版の挙動確認自体ができなくなる店舗が続出するため見送った（`WarningCode.php`のコメント参照）。正しい修正は`ProductReader`側で税込基準へ正規化することだが本PR差分範囲外 | 未起票 |
| 2026-09-15 | e2-3-push-product/G1-duplicate-on-retry | High（対象外・要判断） | includes/Adapters/ColorMe/ColorMeAdapter.php:push_product / includes/Support/RateLimitExhaustedException.php | 商品POST/PUTで既にremote_idが確定した後（追いPUT・バリエーション・画像のいずれか）に`RateLimitExhaustedException`が再スローされる、またはPOST応答にidが無く例外化されると、`push_product()`は`PushResult`を返さないまま失敗し、`Exporter`はmappingを一切書き込まない。次回exportはこのローカル商品を「未エクスポート」として扱い再度POSTするため重複商品が作成されうる（Codex P1・Copilot Suppressed comments、複数箇所で指摘）。正しい修正には`RateLimitExhaustedException`（または類する仕組み）が部分成功のremote_idを`Exporter`まで運べる契約拡張が必要で、`PlatformAdapter`/`RateLimitExhaustedException`の公開契約変更を伴うため本PRの差分範囲を超える | 未起票 |
| 2026-09-15 | e2-3-push-product/G1-stale-mapping-on-404 | Medium（対象外・要判断） | includes/Adapters/ColorMe/ColorMeAdapter.php:push_product | 既存mappingが指すColorMe側商品が削除済みの場合、更新PUTは404になり`Exporter`は当該商品を1件失敗として記録するがmappingは削除されない。以後の全exportが同じ削除済みIDへPUTし続け、その商品は二度とexportされない（Codex/Copilot指摘）。404を検知して新規作成へフォールバックし、`Exporter`が新remote_idで置き換えられるようにする設計が必要（`PushResult`は現状remote_id変更に対応済み—PR #40 G3—だが、404から新規作成への切替はアダプタ側の実装が必要） | 未起票 |
| 2026-09-15 | e2-3-push-product/G1-update-omits-clear | Medium（保留） | includes/Adapters/ColorMe/Transform/ProductTransformer.php:to_update_payload/base_payload | Woo側でSKU/説明文等がnullへ変わった場合、`base_payload()`はそのフィールドをpayloadから省略する（ColorMeが「省略されたフィールドは既存値を保持する」という未実証の前提に依拠）。この前提が正しければ、Woo側でクリアした値がColorMe側に古いまま残り続ける（Codex指摘）。実機確認（このdev-cycleの後続ステップ）でPUTの部分更新セマンティクスを検証してから対応要否を判断すること | 未起票 |
| 2026-09-15 | e2-3-push-product/G1-capability-change-stale-checksum | Medium（保留） | includes/Adapters/ColorMe/ColorMeAdapter.php:push_product | 非プレミアムプラン時に`PRODUCT_IMAGES_NOT_PUSHED`（retry対象外）を積んだ商品は、後でプレミアムへアップグレードしても（Canonicalの内容が変わっていないため）checksum一致でスキップされ続け、画像が永久にpushされない（Codex指摘）。プラン変更を検知してchecksumを無効化する仕組みが必要 | 未起票 |
| 2026-09-15 | e2-3-push-product/G1-name-length-validation | Low | includes/Adapters/ColorMe/Transform/ProductTransformer.php:base_payload | ColorMeの商品名は100文字上限（swagger）だが、dry-run/読出時点でこれを検証していないため、超過する商品は実行時にのみ422で失敗する（`DryRunPlatformWriter`はアダプタを呼ばないため気付けない。Codex指摘）。dry-run時点での長さ検証を検討すること | 未起票 |
| 2026-09-15 | e2-3-push-product/G1-L1 | Low | includes/Adapters/ColorMe/Transform/ProductTransformer.php:to_push_amount | `Cast::to_int_or_null()`は小数を切り捨てる（`100.99`→`100`）ため、Woo側で小数価格が設定されていた場合に無警告で金額が変わる（Copilot Suppressed comments指摘）。JPY運用では実害小と判断し見送ったが、他ASP対応時は要検討 | 未起票 |
| 2026-09-15 | e2-3-push-product/G1-L2 | Low | includes/Sync/Exporter.php:variant mapping書き戻し | 個々のvariant_remote_idが空文字列で、かつアダプタが対応する警告を一切積まない（真の契約違反）場合、親商品のcount一致チェックだけでは検出できず親checksumがキャッシュされうる。今回は「countが一致しない」パターンのみ対処し、「countは一致するが一部が空文字列で警告も無い」パターンは未対処（Copilot Suppressed comments指摘） | 未起票 |
| 2026-09-15 | e2-3-push-product/G2-image-rate-limit | Low（既出） | includes/Adapters/ColorMe/ColorMeAdapter.php:push_images | `G1-name-length-validation`の直前にある`e2-3-push-product/R1-L4`と同一指摘（`POST /images`独自のレート制限10秒5リクエストが`RateLimiter`でモデル化されていない）。Codexが再度指摘（dbid=4010655931）。対応方針はR1-L4のまま変更なし | 未起票 |
| 2026-09-15 | e2-3-push-product/G2-terminal-warnings-not-surfaced | Medium（対象外・既存制約） | includes/Sync/Exporter.php / includes/Sync/Importer.php | `*_PUSH_FAILED`（終端・retry対象外）警告は実行(非dry-run)exportでは個別警告として永続化されない（`Sync\Importer::process_items()`と同じ既存方針。CLAUDE.md「実移行の結果レポートに個別の警告は残らない」参照、PR #37で意図的にdetail追加を取り消した実績あり）。Codex指摘（dbid=4010655938）はこの既存制約の再指摘で、本PR固有の問題ではないため対象外とする | 未起票 |
| 2026-09-15 | e2-3-push-product/G2-variation-sale-price | High（対象外・要判断） | includes/Woo/Reader/ProductReader.php:variants / includes/Adapters/ColorMe/ColorMeAdapter.php:push_variant_details | `ProductReader::variants()`は`\$variation->get_regular_price()`のみを`CanonicalProduct::\$variants[].price`へ格納し、セール価格（`is_on_sale()`）を運ぶフィールドが無い。そのため`push_variant_details()`はバリエーションがセール中でも定価を`option_price`として送ってしまう（単純商品側は`push_prices()`で正しくセール価格を出し分けているのと非対称。Codex指摘, dbid=4010655940, P1）。修正には`ProductReader`（本PR差分範囲外・既存コード）へのセール価格フィールド追加と、ColorMe側`option_market_price`等の対応フィールド調査が必要 | 未起票 |
| 2026-09-15 | e2-3-push-customer/R1-X1 | Medium（対象外・既存コード起因） | includes/Woo/Support/AddressMapper.php:to_woo() | ColorMeの海外顧客（`pref_id=48`）をインポートすると`state=''`/`country=''`でWooに保存される（Phase 1で実装・レビュー済みの既存コード）。このため`push_customer()`で該当顧客をexportすると`pref_id_from_state('colorme', null, null)`が`null`を返し、必須フィールド欠落として恒久的にスキップされる（海外顧客がexportできない往復欠損）。dry-runでもアダプタ未呼出のため理由がレポートに残らない。修正には`AddressMapper::to_woo()`（本PR差分範囲外）の海外情報保存方式の見直しと、既にインポート済みの顧客の扱い（バックフィル要否）の検討が必要 | 未起票 |
| 2026-09-15 | e2-3-push-customer/G1-duplicate-on-retry | High（対象外・要判断） | includes/Adapters/ColorMe/ColorMeAdapter.php:push_customer | 新規作成POSTが2xxで`customer.id`を含まない場合、`push_customer()`は`RuntimeException`を投げて失敗し、`Sync\Exporter`はmappingを一切書き込まない。次回exportはこのローカル顧客を「未エクスポート」として扱い再度POSTするため重複顧客が作成されうる（Copilot指摘, dbid=4011402600）。`e2-3-push-product/G1-duplicate-on-retry`と同根の限界で、正しい修正には`PlatformAdapter`/`PushResult`の部分成功remote_id伝搬という契約拡張が必要なため本PRの差分範囲を超える | 未起票 |
| 2026-09-15 | e2-3-push-customer/G1-optional-field-validation | Low（保留・設計判断） | includes/Adapters/ColorMe/Transform/CustomerTransformer.php:base_payload | `furigana`（文字種・50文字）/`hojin`/`busho`（長さ）/`birthday`（date形式）等、swaggerが定める任意項目の書式制約を事前検証していない（Copilot Suppressed comments指摘）。`mail`/`name`の非空性もWP側の保証（`wp_insert_user()`のメール検証、`display_name`が空にならない`CustomerReader::name()`のフォールバック）に依拠し明示検証していない（Copilot指摘, dbid=4011402624）。`push_product()`も商品名・説明文の長さ等を同様に事前検証していないのと同じ設計方針（必須フィールドの欠落のみフェイルクローズし、それ以外の書式違反はColorMe側の422に委ねVALIDATION_EXCEPTIONとして扱う）。`name`のmaxLength=50のみ実害が大きい（必須項目のため422が永久ループする）と判断しG1で対応済み（`to_create_payload()`）。他のフィールドで実店舗検証時に問題が顕在化した場合に再検討する | 未起票 |
| 2026-09-15 | e2-3-push-customer/G1-name-length-on-update | Low（保留） | includes/Adapters/ColorMe/Transform/CustomerTransformer.php:base_payload | `to_update_payload()`は`name`を無条件に送るため、Woo側で表示名が50文字を超えて変更された既存顧客の更新は422になり続ける（`to_create_payload()`のみname長を検証、G1で対応）。更新は必須項目が無くフェイルクローズの対象外な設計のため、name超過時は他フィールドのみ更新し`name`を省略する対応も考えられるが、実害（既存顧客の表示名が事後的に50文字を超えて編集される頻度）は小さいと判断し見送った | 未起票 |
| 2026-09-15 | e2-3-push-customer/G1-stale-mapping-on-404 | Medium（対象外・要判断） | includes/Adapters/ColorMe/ColorMeAdapter.php:push_customer | 既存mappingが指すColorMe顧客が削除済みの場合、更新PUTは404になり`Exporter`は当該顧客を1件失敗として記録するがmappingは削除されない。以後の全exportが同じ削除済みIDへPUTし続け、その顧客は二度とexportされない（Copilot Suppressed comments・Codex両方が指摘, dbid=4011477781）。`e2-3-push-product/G1-stale-mapping-on-404`と同根の限界で、404を検知して新規作成へフォールバックする設計が必要なため本PRの差分範囲を超える | 未起票 |
| 2026-09-15 | e2-3-push-customer/G2-clear-vs-unavailable-on-update | Medium（保留・設計判断） | includes/Adapters/ColorMe/Transform/CustomerTransformer.php:to_update_payload | `to_update_payload()`はnullの任意項目（`hojin`/`busho`/`furigana`/`birthday`/`other`/`address2`）を省略し「ColorMe側の既存値を保持する」設計だが、これは「値が不明」と「Woo側で意図的にクリアされた」を区別できない。店舗がWoo側でこれらの値を意図的に空にした場合、次回exportはchecksum変化により実際にpushされるが、payloadからは省略されるためColorMe側の古い値が残り続け、かつchecksumはクリア後の状態でキャッシュされるため以後は再試行すらされない（Codex指摘, dbid=4011595410）。同じ「省略=既存値保持」パターンは`ProductTransformer::to_update_payload()`の任意項目にも共通する可能性がある設計判断のため、`CanonicalModel`契約に「意図的なクリア」を表現する手段（例: 明示的なクリアの印）を追加する設計が必要。本PRの差分範囲を超えるため見送る | 未起票 |
| 2026-09-15 | e2-3-push-customer/G3-dry-run-validation-parity | Medium（保留・既知の限界） | includes/Woo/Export/DryRunPlatformWriter.php | dry-runはアダプタを一切呼ばずmappingsの有無だけでcreated/updatedを判定するため、必須項目欠落（住所/電話番号不足等）で実行時にはスキップされる顧客もdry-runでは`created`として表示され、店舗に誤った移行可否のプレビューを提示しうる（Codex指摘, dbid=4011680617）。`push_product()`の各種`*_INCOMPLETE`警告も同様にdry-runで検出できないことは本PR内で複数回文書化済みの既知の限界。是正にはdry-run経路でもアダプタ側のペイロード検証ロジック（`to_create_payload()`相当）を呼べるようにする設計拡張が必要で、輸入方向の`Woo\Writer\EntityWriter::validate()`と同種の仕組みをexport方向にも設けることになる。E2-4（エクスポートUI+往復E2E）で扱うべき論点と判断し見送り | 未起票 |
