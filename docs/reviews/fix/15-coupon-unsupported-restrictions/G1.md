# ゲートラウンド G1
- 対象 HEAD: 38f8d24（依頼時刻 2026-09-12T12:27:12Z）
- Copilot: 新規 5 件（inline 3 / suppressed 2） / Codex: **応答なし（15分TIMEOUT）→ G2 で `@codex review` 再依頼**

## 指摘
### [G1-1][Copilot][Medium][includes/Canonical/CanonicalCoupon.php:41]
`false` の契約に「Wooの制限軸へ変換済み」を含めていたが、`CanonicalCoupon` に商品ID・カテゴリ・メール制限を
運ぶフィールドが無く `CouponWriter` も `set_product_ids()` 等を呼ばないため、その状態は現時点で成立しない。
将来のアダプタが「Wooに機能がある」だけを根拠に `false` を立てると制限が落ちた無制限クーポンが保存される。
判定: 修正
**対応:** v1.0 時点で `false` にしてよいのは「ASP側に制限が無い」場合のみ、と契約を絞った（6865478）。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/37#discussion_r3996211085

### [G1-2][Copilot][Medium][includes/Canonical/CanonicalCoupon.php:91]
`from_array()` の `(bool)` キャストにより `'0'` が `false`（＝保存してよい、という宣言）に、`'false'` が `true` に
化け、フェイルクローズを迂回しうる。判定: 修正
**対応:** `is_bool()` で実bool以外をすべて `null`（不明）へ倒す。`'0'`/`'false'`/`1`/`[]` の4ケースをテスト追加（6865478）。
`php -r 'var_dump((bool)"0", (bool)"false");'` → `false` / `true` で指摘の前提を実測確認済み。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/37#discussion_r3996211096

### [G1-3][Copilot][Medium][docs/03-design-decisions.md:113]
R1-3 で PHP 側 docblock を是正した「Wooで表現できない」→「Wooのクーポン設定へ写せない」が設計ドキュメントに
横展開されていなかった（CLAUDE.md の横展開ルール違反）。判定: 修正
**対応:** 設計ドキュメントを実装側の定義に合わせた（6865478）。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/37#discussion_r3996211111

### [G1-4][Copilot][Low][CLAUDE.md:72]（suppressed comment・スレッドなし）
同じ横展開漏れが CLAUDE.md のバレットにも残っていた。判定: 修正
**対応:** G1-3 と同じ文言に更新（6865478）。

### [G1-5][Copilot][Low][includes/Woo/WarningCode.php:80]（suppressed comment・スレッドなし）
警告コード `coupon_group_limit_unsupported` の改名は「安定キー」の契約を破り、Pro側・保存済みCSVの利用者が
旧コードを認識できなくなるため、旧コードを後方互換で残すか移行経路を用意すべき、という指摘。判定: **保留（却下）**
**理由:** 本プラグインは wordpress.org 未公開（v1.0 前）で Pro 版アドオンも未存在。旧コードの参照は
リポジトリ全体で 0 件（PHP/TS/po/pot/json/docs を grep 済み）。`WarningCode` docblock の「i18nしない安定キー」は
「表示文言ではなく識別子として安定」の意味であり、バージョン間不変の外部契約を宣言したものではない
（コードは dry-run CSV に生値で出るのみで、厳密一致で参照する消費者は `includes/` 内に存在しない）。
旧コードを残すと「商品グループ制限」限定の誤った名前が恒久的に残り、改名の目的（フィールドが商品グループに
限定されなくなったことの反映）そのものを損なう。

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Copilot | 1 | 5（修正4 / 保留1） | 未収束（G2 で再依頼） |
| Codex | 1（自動レビュー未発火） | — | 未応答。G2 で `@codex review` にて再依頼 |

修正後HEAD: 6865478
