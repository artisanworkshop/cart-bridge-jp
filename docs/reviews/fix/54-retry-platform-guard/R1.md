# R1 レビュー結果
- 対象: main(13b692c)...a82046f
- 判定: APPROVE

自己レビュー（絶対ルール直行チェックリスト含む）+ 独立サブエージェント（`general-purpose`/opus。
`git diff main...HEAD` をファイルに書き出し、CLAUDE.md・`.claude/rules/sync-export-tools.md`・
`.claude/rules/frontend.md`・`docs/review-baseline.md`・`docs/review-backlog.md` を読ませた上で
`JobManager::retry()`/`start_run()`/`process_job()`/`complete_job_and_advance()`・
`RestController::retry_job()`・`ImportTab.tsx`/`ExportTab.tsx`・新規テストの本体を実際に
トレースさせた敵対的レビュー）を併用。両者ともCritical/High/Medium指摘なし。

## 指摘
なし（Critical/High/Medium）。

## 対象外指摘（差分範囲外）
- [R1-X1][Low][src/tabs/ExportTab.tsx:1108-1113] R1-L2のうち`ExportTab.tsx`側の同種コメント
  （`JobManager::retry()`が`start_run()`のガードを経由せずrequeueする、という記述）も本PRの
  バックエンド修正で古くなったが、この行自体は本PRの差分に含まれない既存コード（PR #53）のため
  修正しない。backlogへ追記。

## Low（backlogへ・一部は即修正）
- [R1-L1][Low][includes/Sync/JobManager.php:148-157] `retry()`のガードは「判定→更新」の非原子的操作で、
  ほぼ同時の`retry(A)`/`start_run(B)`や、同一失敗ジョブへの2重Retryはこのガードだけでは防げない
  （`start_run()`自身が元から持つ同種の競合と同質。今回の差分で新たに持ち込んだものではない）。
  → 修正せずbacklogへ。
- [R1-L2][Low][src/tabs/ImportTab.tsx:597-602,655-657] 追加したコメントが「`JobManager::retry()`が
  `start_run()`のガードを経由せずrequeueする」という、本PR自身のバックエンド修正で解消済みの
  旧状態を説明していた（自己矛盾）。→ **本PRの差分内（自分が書いたコメント）のため即修正**
  （サーバー側は409を返すようになったが、無用なエラー表示を避けるためフロント側でも先に止める、
  という現状に合わせた説明に更新）。
- [R1-L3][Low][docs/reviews/fix/54-retry-platform-guard/dev-cycle.md] 計画承認ログにユーザー名入りの
  ローカル絶対パスを記載していた。`docs/review-backlog.md`の`e2-4-export-ui-e2e/G1-copilot-plan-path-precedent`
  で「今後は書かない運用にする」と既に指摘されているのと同型のため、**即修正**（ファイル名のみの記載に変更）。
- [R1-L4][Low][tests/unit/Sync/JobManagerTest.php] 新規ガードの境界ケース（別プラットフォームでは
  ブロックされない・別runがpaused状態でも409になる）のテストが無い。→ 修正せずbacklogへ（任意）。

## スコープ外の確認事項
サブエージェントに、issue #54完了条件item4の「`cancel_run()`の競合（`f1-6-import-ui/R1-X1`）を
本PRに含めるか」を再検証させた。`retry()`の今回の修正は`cancel_run()`を直さなくても単体で安全
（別種の競合であり、本修正が依存・悪化させる箇所はない）ことを確認。計画時の「別issueのまま」
という判断を覆す新しい技術的理由は見つからなかった。

## 品質チェック
`composer lint && composer analyze && composer test:wpenv`（1079テスト）+ `npm run lint && npm run build`
すべてgreen（R1-L2/L3修正後に再実行済み）。

修正後HEAD: 3243c25
