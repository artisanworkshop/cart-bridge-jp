# G2 ゲートラウンド（Copilot 2 回目 / Codex 2 回目）
- 対象 HEAD: 38a9def（G1 の修正 `f9eee57` ＋ 記録の docs コミット）。CI は 4 ジョブ green の状態で依頼
- 依頼: 2026-09-20T14:16Z に両ボットへ（Copilot はレビュアー指名、Codex は `@codex review`）
- 応答: Copilot 14:20Z（判定 🟡 Changes recommended。インライン 1 件）／Codex 14:21Z（インライン 1 件）。レビュー本文（系統 B）に追加の指摘なし
- 確認ゲート: ユーザーが「commit・push・返信する」を選択（Codex P2 のコード根本対応は選ばず、文書化＋backlog）

## 仕分け
| # | 出所 | 場所 | 指摘（要旨） | 判断 | 対応 |
|---|---|---|---|---|---|
| G2-1 | Copilot（dbid 4057158034） | `PlatformAdapter.php:104` | 必須メソッドの追加で外部アダプタが fatal。任意インターフェースにすべき | **G1-2 と同一の指摘**。メンテナー判断で**現状維持** | G1 で確認ゲートを通して確定済みの設計判断（承認済み計画の逸脱候補 1）。同じ理由を返信し、スレッドは未解決のまま |
| G2-2 | Codex P2（dbid 4057161719） | `PrefStateRepair.php:150` | 旧インポートで email 突合により採用した既存アカウントに対して「Sample data cleanup」を先に実行すると、`SampleCleanup::remove_customer()` が `_cbjp_platform`/`_cbjp_remote_id` を外し mapping も消すため、本ツールが列挙できず、誤った state が残ったまま「要補正なし」と表示される | **事実として妥当。ただし根本対応は範囲外**（対応方針: 文書化＋backlog） | 実コード確認済み（`SampleCleanup.php:359-366`）。ASP の ID を失ったアカウントを後から修復する手段は無く、出自を残す解決は `SampleCleanup` の契約変更を伴うため本 PR では行わない。UI の説明文に「Run this before “Sample data cleanup”」を明記、docs/03 §10.3 の既知の制限 (6) に追記、backlog（`G2-cleanup-loses-provenance`）へ。PR 本文の「既知の制限」にも追記。スレッドは未解決のまま返信 |

## 品質チェック（修正後）
`quality.sh` all green: PHPUnit 1065 tests, 3783 assertions / PHPCS / PHPStan / `npm run lint` / `npm run build`（PHP のロジック変更なし）。

## 記録
- 修正 commit: （push 後に追記）
- Resolve したスレッド: なし（G2-1 は既決の設計判断の重複、G2-2 は根本対応を見送って文書化のみのため、いずれも返信のみで未解決のまま）
