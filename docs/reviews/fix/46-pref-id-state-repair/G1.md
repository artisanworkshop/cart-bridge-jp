# G1 ゲートラウンド（Copilot 1 回目 / Codex 1 回目）
- 対象 HEAD: 9e6b166（レビュー対象コミット）
- 依頼: Copilot は 2026-09-20T12:51Z に指名、Codex は PR 作成時の自動レビューが 15 分以内に発火しなかったため 13:07Z に `@codex review` で再依頼（この場合も Codex の依頼回数は 1 回目として数える）
- 応答: Copilot 12:54Z（判定 🟡 Changes recommended。インライン 1 件、本文の Suppressed comments なし）／Codex 13:10Z（インライン 3 件）
- 取得: 系統 A（スレッド）4 件、系統 B（レビュー本文）は追加の指摘なし
- 確認ゲート: ユーザーが「現状維持（返信して保留）」「commit・push・返信する」を選択

## 仕分け
| # | 出所 | 場所 | 指摘（要旨） | 判断 | 対応 |
|---|---|---|---|---|---|
| G1-1 | Copilot（dbid 4056983716） | `PrefStateRepair.php:214` | 所有判定が可変の `_cbjp_platform` のみ。`CustomerWriter` は email 採用の既存アカウントにも書くので、不変マーカー `_cbjp_created_by_import` で絞るべき | **不採用**（説明文は不正確だったので下記 G1-4 で修正） | 実コード確認: `CustomerWriter` は採用した既存アカウント（保護ロール以外）の住所も上書きするため、その誤った state もインポートが書いたもので修復の対象。`_cbjp_created_by_import` は「誰が作成したか＝削除してよいか」の判定用で、「誰が住所を書いたか」には合わない。絞ると採用アカウントの誤りが直らなくなる。コードコメントと docs/03 に理由を明記。スレッドは未解決のまま返信のみ |
| G1-2 | Codex P1（dbid 4057015250） | `PlatformAdapter.php:104` | 抽象メソッド追加が外部/Pro のアダプタ実装を fatal にする。任意インターフェースにすべき | **設計判断→ユーザー確認**。**現状維持（保留）** | 承認済み計画の「逸脱候補 1」・PR 本文の「設計ドキュメントからの逸脱 1」そのもの。実装は ColorMe のみ・v1.0 未公開・Pro 版は上限解除でアダプタを足さない・E2-1 の `mapping_candidates()` と #45 の `push_order()` 署名変更に前例。スレッドは未解決のまま返信のみ（最終判断はマージ時にユーザーが行う） |
| G1-3 | Codex P2（dbid 4057015264） | `PrefStateRepair.php:404` | 現在の国を検証に含めていない。国だけ非日本へ変更済みの実体に `JPxx` を書くと矛盾した住所になる | **採用（修正）** | 現在の国（顧客は `{side}_country`、受注は `get_*_country('edit')`）を `AddressMapper::to_woo()` の期待値と突き合わせ、不一致は `unverified`。テスト `test_a_record_whose_country_was_changed_away_from_the_platform_country_is_left_alone`（国の判定を外すと落ちることを変異で確認）。docs/03 追記 |
| G1-4 | Codex P2（dbid 4057015274） | `ToolsTab.tsx:926` | 「Only records created by the import are touched」は不正確（採用アカウントも対象になる）。文言を直すか不変マーカーで絞るべき | **採用（文言を修正）** | G1-1 と同根。「Only records that this plugin imported are touched, including existing accounts it matched by email and updated」へ。スキップの説明文も「was not imported or updated by this plugin」へ |

## 品質チェック（修正後）
`quality.sh` all green: PHPUnit 1065 tests, 3783 assertions / PHPCS / PHPStan / `npm run lint` / `npm run build`。

## 記録
- 修正 commit: （push 後に追記）
- Resolve したスレッド: G1-3, G1-4（修正したもののみ）。G1-1, G1-2 は返信のみで未解決のまま
