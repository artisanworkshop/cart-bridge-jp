# R2 検証結果
- 対象: 708b860...07c3931（R1 の修正差分のみ）
- 判定: APPROVE（R1 の Medium 2 件・Low 3 件はすべて解消。R1 修正で新たに混入した Critical/High/Medium は無い）
- 検証方法: フレッシュコンテキストの独立サブエージェント（opus）。ミューテーション実測を含む。作業ツリーに変更が残っていないことを実装者側でも確認（`git status` が未追跡の `docs/reviews/...` のみ）

## R1 指摘の解消判定
| ID | 重大度 | 判定 | 根拠 |
|---|---|---|---|
| R1-1 | Medium | 解消 | 新テストは `tax_type=included` + `sale_price='abc'`。`to_push_amount()` は通常価格 2200 を返し `'abc'` を null にするため、`push_variant_details()` の「実売価格だけ換算できない」分岐を確実に通る。分岐に `elseif ( null !== $regular ) { option_price = $regular }` を一時的に足すと当該テストが失敗することをミューテーションで実測（確認後に元へ戻し、shasum 一致・`git diff --stat` 空を確認） |
| R1-2 | Medium | 解消 | `SALE_END_DATE_NOT_PUSHED` はセールを実際に送る経路だけで積む（単純商品は `$inclusive_sale !== null`、バリエーションは `sale_price` を設定する同じブロック）。`is_on_sale('edit')` は終了日が過去/開始日が未来で偽になるため判定と整合。`with_detail()`/`split()` の形式どおり。`indicates_export_blocking()`/`indicates_unresolved_reference()` には入れておらず、warnings は checksum の対象外なので再 push 頻度に影響しない |
| R1-L3 | Low | 解消 | コメントを実挙動に合わせ、部分ケース（1 バリエーションだけ換算不能）のテストで variants=`['M']`・`:detail` 付き blocking 警告・`ALL_VARIATIONS_EXCLUDED` 無し・`indicates_export_blocking()===true` を検証 |
| R1-L4 | Low | 解消 | `is_on_sale('edit')===true` を前提アサートした 0 円セール価格のテスト（`> 0` ガードを外すと失敗する）。docblock 追記は実挙動と一致 |
| R1-L5 | Low | 解消 | `array_merge( $warnings, [...] )` で先行警告を保持 |

## 新規指摘（R1 修正で新たに混入したもののみ）
なし（Critical/High/Medium はゼロ）。

参考（Low 未満・対応不要）: 別バリエーションが換算不能で商品全体が blocking になっても、`SALE_END_DATE_NOT_PUSHED:{id}` は積まれたまま残る。実際には push されないため dry-run 上は冗長な情報警告になるだけで害は無い。

## 対象外
なし（R1-X1/X2・R1-L1/L2 は backlog 登録済み）。

## 確認した点
`get_date_on_sale_to()` は `WC_DateTime|null` を返し、バリエーションは自身の `_sale_price_dates_to` を読む（親から継承しない）。時刻依存のテスト（`time()+WEEK_IN_SECONDS`）は安定。`_insert_tax_rate` はロールバックで後始末される既存パターンと同じ。`src/` に警告ラベル表は無い。`composer lint` OK / `composer analyze` No errors / `composer test:wpenv` OK（1109 tests, 3903 assertions）。

修正後HEAD: 07c39310dab7a22013987d5711544f53f8c19445（R2 での追加修正なし）
