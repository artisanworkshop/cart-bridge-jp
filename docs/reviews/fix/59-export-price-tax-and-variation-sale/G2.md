# ゲートラウンド G2
- bot: Copilot（2 回目）。Codex は G1 で収束済みのため依頼せず
- 対象 HEAD: e9f56a7（依頼時刻 T=2026-09-24T04:28:29Z。CI green）
- Copilot: 新規 1 件（本文の「Previously missed」。インライン 0 件、判定 🔵 Needs a closer look）/ Codex: 依頼せず（収束済み）

## 指摘
本文の「Open (3)」は G1 で返信済み・未解決のスレッド 3 件の再掲（リンクの `#discussion_r4089899212` / `r4089899181` / `r4089899143` が G1-3 / G1-2 / G1-1 の既存スレッドの dbid と一致）。新規ではなく重複として扱う（G1 で保留判断済み）。

### [G2-1][Copilot][Low][includes/Adapters/ColorMe/ColorMeAdapter.php:1101]（本文指摘・スレッド無し）
実売価格を換算できないとき、価格フィールドを両方省いたままバリエーション更新を送るため、ColorMe 側に以前保存された販売価格・定価が残り、フェイルクローズになっていない。`null` を明示して消す（またはバリエーション更新自体を失敗させる）べき。
判定: **保留**（指摘の方向が金銭的に逆）。実フィクスチャ `tests/fixtures/colorme/product_option_detail.json` で、`option_price: null` のバリエーションは商品レベルの価格にフォールバックする（商品 `sales_price_including_tax: 3080` に対し、バリエーションの `option_price: null` でも `option_price_including_tax: 3080`）。variable 商品の親の価格は「最安の定価」のため、`null` で消すとより高いバリエーションの売価が最安価格へ落ちて誤る。省略（ColorMe 側の既存値を保持）が安全側。到達できる経路も限られる（`Woo\Reader` は換算不能なら `sale_price` を運ばずバリエーションごと除外する。この分岐に入るのは外部境界のCanonicalが非数値の `sale_price` を運んだ場合のみ）。`docs/03` §10.2 に「省く＝既存値を保持し、`null` 明示は採らない理由」を追記した。
**対応:** docs に理由を追記（コード変更なし）/ 本文指摘のためスレッドは無く、この記録と PR サマリコメントが処理済みマーカー

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Codex | 1 | 0 | 収束（G1） |
| Copilot | 2 | 1（Low・保留）＋G1 の 3 件の再掲 | 未収束 |
