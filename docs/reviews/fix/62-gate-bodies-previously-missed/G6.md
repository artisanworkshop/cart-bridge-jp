# ゲートラウンド G6
- bot: Copilot（3 回目・最終）
- 対象 HEAD: c92814c の次の docs コミット（依頼時刻 T=2026-09-24T10:04:02Z。CI green）
- Copilot: 新規 1 件（インライン 1 件、判定 🟡 Changes recommended、`Findings: 1 [Medium]`）/ Codex: 依頼せず（3 回に到達済み）
- 本文（`gate-bodies.sh` の整形出力）: `Open (1)` は今回の新規スレッド（`· New`）のみ。`Previously missed` なし

## 指摘
### [G6-1][Copilot][Medium][.claude/skills/cbj-dev-cycle/scripts/gate-bodies.sh:43]
パーサーは未知の `<details>` 節を出す（fail-open。指摘を握りつぶさない）と明示しているのに、回帰テストがこの挙動を検証していない。未知の節（例: `Resolved since last review`）を含む fixture を追加し、見出しと本文の両方をアサートしないと、将来のパーサー変更で新規指摘が落ちても既存の fixture が全て通ってしまう。
判定: **修正**（妥当。この PR が掲げる「指摘を握りつぶさない」の中核の約束が未テストだった）。
**対応:**
1. 実物の fixture `copilot-v2-resolved-since-last-review.md`（本 PR の G4 の Copilot レビュー本文。未知の節 `Resolved since last review (1)` と `Previously missed (1)` が同居）を追加し、見出し・項目と dbid・`Findings: None`・同居する `Previously missed`・`path:line`（ゼロ幅スペース除去済み）をアサート。
2. 合成の fixture `copilot-v2-unknown-section.md`（一度も見たことのない `Some future section (2)` と `What changed in this PR` の表）を追加し、未知の節の見出しと全項目が出ること、ノイズの表は出ないことをアサート。
3. ミューテーション: 未知の節を握りつぶすよう `gate-bodies.sh` を一時的に変更すると 5 件失敗することを確認し、元に戻して全件 ok を確認（`cmp` で復元を確認）。`bash -n` / `shellcheck -S warning` 問題なし。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/64#discussion_r4092360435

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Codex | 3 | 0（G3・G5） | 収束・上限到達 |
| Copilot | 3 | 1（G6-1、修正） | 上限到達（4 回目は依頼しない）。最後の修正は push して CI を確認するのみ |
