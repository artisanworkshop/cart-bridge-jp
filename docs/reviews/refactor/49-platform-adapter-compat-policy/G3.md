# ゲートラウンド G3
- 対象 HEAD: d1c0311（依頼時刻 2026-09-23T22:38:36Z）
- Copilot: 新規スレッド0件、本文指摘1件 / Codex: 新規0件（収束）

## 指摘

### [G3-1][Copilot][本文指摘（スレッドなし）][Medium][tests/unit/Adapters/AbstractPlatformAdapterTest.php:147]
G2で固定した`V1_METHOD_NAMES`定数は、v1.0.0公開前にD20が許容しているはずの「既定実装なしでの新メソッド
追加」まで`test_new_methods_have_default_implementations`を失敗させてしまう（公開前後で基準を切り替える
仕組みが無かった）。判定: 修正
**対応:** `V1_METHOD_NAMES`定数を`v1_method_names()`メソッドに変え、公開前は`BASELINE`のキーと連動する
実装にした。`docs/10-tasks.md` R3-4に公開時点でリテラル配列へ書き換える手順を明記。公開前の正当な追加が
通ること、かつ「凍結後」を模して同メソッドを一時的にリテラル配列へ書き換えてもG1-2のシナリオを引き続き
検出できることの両方を実地確認済み（コミット5ebcf63）。レビュー本文:
https://github.com/artisanworkshop/cart-bridge-jp/pull/58#pullrequestreview-5297617962

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Copilot | 3（上限） | 本文1件（修正済み） | 未確認（依頼上限到達。bot側は時間差で応答している可能性あり） |
| Codex | 3（上限） | 0件 | 収束 |

修正後HEAD: 5ebcf63

依頼回数が両ボットとも3回に達したため、これ以上の依頼はしない（cbj-dev-cycleの絶対ルール）。
