# ゲートラウンド G1
- 対象 HEAD: 4dfa521（依頼時刻 2026-09-23T22:05:01Z）
- Copilot: 新規1件 / Codex: 新規2件

## 指摘

### [G1-1][Copilot][Medium][tests/unit/Adapters/AbstractPlatformAdapterTest.php:161]
`describe_signature()` が `static` 修飾子と戻り値の参照渡し（`returnsReference()`）を比較しておらず、
公開後にこれらを既存メソッドへ追加してもBASELINEと同じ文字列のままCIをすり抜ける。判定: 修正
**対応:** `ReflectionMethod::isStatic()`/`returnsReference()`をシグネチャ文字列に含めた（コミット5625cf9）/
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/58#discussion_r4087792819

### [G1-2][Codex][Medium][tests/unit/Adapters/AbstractPlatformAdapterTest.php:101]
`test_new_methods_have_default_implementations`が新メソッドをBASELINEへ追記した後、その既定実装だけが
シグネチャを変えずに削除される変更を検出できない（BASELINEのキーを「既定実装が要らない側」の判定に
流用していたため）。判定: 修正
**対応:** BASELINEとは独立した固定の`V1_METHOD_NAMES`定数を新設し判定基準を切替。実地に検出を確認
（コミット5625cf9）/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/58#discussion_r4087835780

### [G1-3][Codex][Medium][tests/unit/Adapters/AbstractPlatformAdapterTest.php:161]
G1-1と同一指摘（`static`/`returnsReference()`の未考慮）。判定: 修正（G1-1と同じ対応）
**対応:** コミット5625cf9 / スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/58#discussion_r4087835790

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Copilot | 1 | 1件（修正済み） | 未収束（次ラウンドへ） |
| Codex | 1（PR作成時の自動レビュー） | 2件（修正済み） | 未収束（次ラウンドへ） |

修正後HEAD: 5625cf9
