# ゲートラウンド G1
- PR: #68 / 対象 HEAD: f149dec
- レビュー: [Copilot #5315094015](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#pullrequestreview-5315094015)（🟡 Changes recommended、`Findings: 1 [Medium]`）/ [Codex #5315142417](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#pullrequestreview-5315142417)（インライン 3 件）。いずれも HEAD 7025d32 へのレビュー
- 再依頼: 本ラウンドでは再依頼していない。G1 の依頼は Copilot をレビュアー指名（07:47Z、タイムラインで登録を確認）と、Codex の自動レビューが 5 分来なかったための `bot-wait.sh --codex-nudge` による `@codex review`（07:53Z。依頼 1 回目として数える）

## 指摘
### [G1-1][Copilot Medium][.claude/skills/cbj-dev-cycle/scripts/gate-record.sh:169]
要旨: 生成するサマリの表に各指摘のスレッドの値が無く、PR コメントに `#discussion_r…` のリンクが無い（SKILL.md に「各スレッドの `#discussion_r<dbid>` リンクを含める」規約がある）。
<!-- 判定は 修正 / 保留 / 対応不要 のいずれかで始める。対応不要のスレッドを Resolve するにはユーザー承認を得たうえで判定に 【承認済み】 と書き添える -->
判定: 修正（Codex P2 の G1-4 と同一の指摘）。実コードで確認: 表は ID/出所/場所/内容/処理の 5 列だった。
対応: 表に `スレッド` 列を追加し、パーサーが保持したリンク（`[r<dbid>](URL)`、URL が無ければ `r<dbid>`）を出す。本文指摘は `なし`。同じ dbid を 1 つの指摘に複数回書いても 1 件に畳む。テスト: リンク列・`なし`・URL の無い参照・重複記載。ミューテーション 2 種（リンク列を出さない・URL を付けない）を検出。
コミット: f149dec
スレッド: [r4102412888](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#discussion_r4102412888)
<!-- 原文: The generated summary table drops each finding's `スレッド` value, so the PR comment has no `#discussion_r...` link for the reviewer to open. The round-summary convention explicitly requires per-thread links (SKILL.md:112); include the parsed thread link/dbid as a column or link, retaining `なし` for body -->

### [G1-2][Codex P1][.claude/skills/cbj-dev-cycle/scripts/test-gate-record.sh:13]
要旨: `WORK=$(mktemp -d)` が `set -e` 下の単独代入で、失敗すると診断を出さずに終了する（`.claude/rules/skill-scripts.md` の規約。新しく書くテストスクリプトにも適用する）。
<!-- 判定は 修正 / 保留 / 対応不要 のいずれかで始める。対応不要のスレッドを Resolve するにはユーザー承認を得たうえで判定に 【承認済み】 と書き添える -->
判定: 修正。実コードで確認: 同じファイルの `HERE=$(…)`・`SHA=$(git rev-parse HEAD)` も同じ形だった。
対応: `WORK=$(mktemp -d)` と、同じファイルの `HERE=$(cd … && pwd)`・`SHA=$(git rev-parse HEAD)` の 3 つを `if ! X=$(…) || [ -z "$X" ]; then <理由>; exit 1; fi` の形に直した（同一ファイル内の横展開）。`mktemp` を失敗させるスタブで「could not create a temporary directory (mktemp failed)」と出て exit 1 になることを実測。兄弟の `test-gate-turn.sh`・`test-gate-bodies.sh` の同種の代入は本 PR の対象外のため `docs/review-backlog.md` に Low として記録。
コミット: f149dec
スレッド: [r4102451067](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#discussion_r4102451067)
<!-- 原文: **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub> Preserve mktemp failures in the test harness** When `mktemp -d` fails, this standalone assignment under `set -e` terminates the test before it can print a diagnostic, leaving CI with only an unexplained exit statu -->

### [G1-3][Codex P1][.claude/skills/cbj-dev-cycle/scripts/gate-record.sh:126]
要旨: `スレッド:` の欄を消す・壊すと `check` が通り、本文指摘として扱われて `replies` が返信・Resolve のコマンドを出さない（生きているスレッドが黙って落ちる。フェイルオープン）。
<!-- 判定は 修正 / 保留 / 対応不要 のいずれかで始める。対応不要のスレッドを Resolve するにはユーザー承認を得たうえで判定に 【承認済み】 と書き添える -->
判定: 修正。実コードで確認: 検証していたのは dbid の重複だけで、dbid が 0 件の指摘は本文指摘と区別できなかった。
対応: `スレッド:` に `discussion_r<dbid>` のリンクか、本文指摘を示す `なし` で始まる値のどちらかを必須にし、無ければ `check`/`summary`/`replies` が非ゼロで止まる。テスト: 行の削除・空・意味のない値の 3 ケース（`なし` は既存の本文指摘のケースで受理を確認）。ミューテーション 2 種（検査を外す・`なし` 以外も本文指摘として通す）を検出。
コミット: f149dec
スレッド: [r4102451077](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#discussion_r4102451077)
<!-- 原文: **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub> Reject findings that lose their thread reference** If an editor accidentally deletes or corrupts a generated `スレッド:` link, `check` still returns success because only duplicate dbids are validated. The finding is t -->

### [G1-4][Codex P2][.claude/skills/cbj-dev-cycle/scripts/gate-record.sh:169]
要旨: 生成するサマリの表がスレッドの URL/dbid を捨てているため、`gh pr comment` で投稿しても `#discussion_r<dbid>` のリンクが無い（G1-1 と同一の指摘）。
<!-- 判定は 修正 / 保留 / 対応不要 のいずれかで始める。対応不要のスレッドを Resolve するにはユーザー承認を得たうえで判定に 【承認済み】 と書き添える -->
判定: 修正（G1-1 と同一）。
対応: サマリの表に `スレッド` 列を追加し、各行に `[r<dbid>](URL)`（本文指摘は `なし`）を出すようにした。同じ内容の別スレッドの指摘（G1-1）と同一の修正。
コミット: f149dec
スレッド: [r4102451086](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#discussion_r4102451086)
<!-- 原文: **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub> Include discussion links in the generated summary** The generated table discards each finding's parsed thread URL/dbid, so a summary posted with `gh pr comment` contains no `#discussion_r<dbid>` links. This confli -->

## 検証
- `test-gate-record.sh` 201 項目（181 → 201）すべて ok。`test-gate-turn.sh`・`test-gate-bodies.sh` も all ok、`shellcheck -S warning`（全スクリプト）問題なし
- ミューテーション 43 種（新規 5 種: スレッド欄の必須検査・`なし` の判定・リンク列・URL の付与・dbid 重複の畳み込み）をすべて明示的な FAIL で検出し、ファイルが元どおり復元されたことを確認
- **dogfooding の気付き（`gate-record.sh` 自身の不具合・改善点。いずれも作業ツリーで対応済みで、commit・push は次の確認ゲートで承認を得てから）**:
  1. 判定語の境界に使った `\p{Han}` 等は Script_Extensions の意味になり `。` `、` にも一致するため、`判定: 修正。…` を「不明な判定」として拒否していた。この記録の `check` で発覚（フィクスチャは `**修正**。` と太字で書いていたため通っていた）。`\p{Script=…}` に変更し、句読点の回帰テストと変異 1 種を追加
  2. 修正の push 後に `init` を実行すると、レビュー行が現在の HEAD 基準で「なし」になり（実際は 7025d32 へのレビュー）、修正済みスレッドの行番号が `?` になった（どちらもこの記録では手で補った）。`--since` 指定時はレビューを T 以降の提出分として拾い対象 commit を併記するように変更し、SKILL.md に「`init` は指摘を取得した直後（修正の前）に実行する」を追記
  - 結果: `test-gate-record.sh` 209 項目すべて ok、ミューテーション 48 種（新規 5 種）をすべて検出

## 収束判定
| bot | 依頼回数 | 新規スレッド | 状態 |
|---|---|---|---|
| Codex | 1 回（nudge の `@codex review`、07:53Z） | 3 | 未収束（G2 で再確認） |
| Copilot | 1 回（07:47Z） | 1 | 未収束（G2 で再確認） |
