# ゲートラウンド G2
- PR: #68 / 対象 HEAD: 2b3b142
- レビュー: [Codex #5317712850](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#pullrequestreview-5317712850)（対象 2b3b142） / [Copilot #5317737730](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#pullrequestreview-5317737730)（🔵 Needs a closer look、対象 2b3b142）
- 再依頼: 本ラウンドの依頼は Codex（`@codex review`、12:35:56Z、依頼 2 回目）と Copilot（レビュアー指名、12:35:54Z、タイムラインで登録を確認、依頼 2 回目）。本ラウンドでは再依頼していない。応答は Codex 12:39Z（インライン 3 件）、Copilot 12:42Z（インライン 0 件、判定 🔵 Needs a closer look、`Findings: None`、本文の `Previously missed` に 1 件）

## 指摘
### [G2-1][Codex P1][.claude/skills/cbj-dev-cycle/scripts/gate-record.sh:121]
要旨: 指摘が 0 件のラウンド（`gate-threads.sh` にも本文にも指摘が無い）では、`init` が 0 件の骨組みを作るのに、`check`/`replies`/`summary` が `no findings under "## 指摘"` で必ず失敗し、クリーンな最終ラウンドを記録できない。
<!-- 判定は 修正 / 保留 / 対応不要 のいずれかで始める。対応不要のスレッドを Resolve するにはユーザー承認を得たうえで判定に 【承認済み】 と書き添える -->
判定: 修正。実コードで確認: 検証が「指摘が 0 件なら常にエラー」だった。
対応: 「## 指摘」の最初の見出しより前に `指摘なし: <確認した内容>` を書けば 0 件のラウンドとして通すようにした（確認内容が空・未記入のマーカーの残り・指摘との併記はエラー。見出しも `指摘なし` も無ければ従来どおり止まる）。`check` は `OK: 0 findings (指摘なし: …)`、`summary` は表なしで「指摘なし」の行と検証欄、`replies` は返信コマンドなしで成功する。`init` はスレッドが 0 件のとき、未記入のマーカーつきの `指摘なし:` の行を骨組みに置く。テスト 17 項目を追加、ミューテーション 5 種（明示なしで通す・確認内容なしで通す・併記を通す・パーサーが認識しない・0 件でも表を出す）を検出。
コミット: 7ce8289
スレッド: [r4104561136](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#discussion_r4104561136)
<!-- 原文: **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub> Allow clean review rounds to pass validation** When both `gate-threads.sh` and the separately inspected review body contain no findings, `init` deliberately creates a zero-finding record, but this condition makes  -->

### [G2-2][Codex P2][.claude/skills/cbj-dev-cycle/scripts/gate-record.sh:173]
要旨: 本文指摘（スレッド無し）の行は常にスレッド欄が `なし` で、記録に書いた `#pullrequestreview-<id>` のリンクが捨てられる。SKILL.md の規約は各本文指摘に元のレビューへのリンクを含めることを求めており、1 ラウンドに複数のレビューがあると曖昧になる。
<!-- 判定は 修正 / 保留 / 対応不要 のいずれかで始める。対応不要のスレッドを Resolve するにはユーザー承認を得たうえで判定に 【承認済み】 と書き添える -->
判定: 修正。実コードで確認: パーサーは `discussion_r<dbid>` しか拾っていなかった。
対応: パーサーが `スレッド:` の値から `pullrequestreview-<id>` を拾い、表のスレッド欄に `なし（[review <id>](URL)）`（スレッドのある指摘なら両方）と出す。URL が無ければ `review <id>`。骨組みの案内コメントに書き方を追記。必須にはしなかった（ヘッダの `レビュー:` 行に各レビューのリンクがあり、1 レビューなら足りるため）。テスト 3 ケースを追加、ミューテーション 2 種（リンクを出さない・URL を付けない）を検出。
コミット: 7ce8289
スレッド: [r4104561147](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#discussion_r4104561147)
<!-- 原文: **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub> Preserve review links for body-only findings** For a body-only finding, the parser produces no `.links`, so this branch always renders `なし` and discards any `#pullrequestreview-<id>` URL a user might add to the re -->

### [G2-3][Codex P2][.claude/skills/cbj-dev-cycle/scripts/gate-record.sh:128]
要旨: 生成された要旨の行（未記入のマーカーつき）を置き換えずに消すと要旨が空になるが、検証は判定と対応しか見ないため `check` が通り、`summary` が「内容」の欄が空の表を出す。
<!-- 判定は 修正 / 保留 / 対応不要 のいずれかで始める。対応不要のスレッドを Resolve するにはユーザー承認を得たうえで判定に 【承認済み】 と書き添える -->
判定: 修正。実コードで確認: 検証していたのは判定・対応・sha だけだった。
対応: 各指摘の `要旨` が空ならエラー（`要旨 is empty`）にした。テスト 1 ケースを追加、ミューテーション 1 種を検出。
コミット: 7ce8289
スレッド: [r4104561156](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#discussion_r4104561156)
<!-- 原文: **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub> Reject findings with an empty summary** If an editor deletes the generated `要旨: TODO(記入)` line rather than replacing it, the parser leaves `.summary` empty, but validation checks only the verdict and action, so `c -->

### [G2-B1][Copilot Medium][.claude/skills/cbj-dev-cycle/scripts/gate-record.sh:162]
要旨: 「重複コミットの畳み込み（`def uniq`）が `index([$x])` で配列と比べているため既存の要素を見つけず、同じ sha が `対応コミット` に複数回出る（173 行目も同様）」という指摘（Copilot 本文の `Previously missed`。スレッド無し）。
判定: 対応不要（誤検知）【承認済み】。主張の挙動は実測で否定された（ユーザーが「対応不要（誤検知）として記録」を承認）。
対応: jq で `["a","b","a","c","b"] | uniq` が `["a","b","c"]` を返し、`["a","b"] | index(["b"])` が 1 を返すことを実測した（`index` は配列の引数で部分配列を探すので、`[$x]` で正しく動く）。173 行目は reducer ではなく表の行。**ただし、この指摘をきっかけに、畳み込みのテストが弱いことを見つけた**: フィクスチャの全指摘が同一の sha のため、畳み込みが壊れても部分一致のアサーションが通り、ミューテーションの対象にもしていなかった。アサーションを行全体の一致（`grep -x`）に強化し、異なる 2 つの commit を別リポジトリで使うテストと、畳み込みを外すミューテーションを追加して検出を確認した（コードの変更なし）。
コミット: 7ce8289
スレッド: なし（本文指摘。[review 5317737730](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#pullrequestreview-5317737730)）

## 検証
- `test-gate-record.sh` 235 項目（209 → 235）すべて ok。`shellcheck -S warning`（全スクリプト）問題なし
- ミューテーション 57 種（G2 で新規 9 種: 畳み込みの除去・要旨の空・0 件ラウンドの 4 種・表の出し分け・本文指摘のリンク 2 種）をすべて明示的な FAIL で検出し、ファイルが元どおり復元されたことを確認。ドライバが 2 度、リファクタで無くなった旧パターン（`threadcell` への切り出し等）で途中終了したため、パターンを直して残りを再実行した（検出漏れではない）
- Ubuntu 24.04（bash 5.2・jq 1.7・perl 5.38）の Docker コンテナで 3 つの dev-tooling テストが通ることを確認
- Copilot の [Medium]（畳み込み）は主張が誤り（jq の実測）だが、テストの弱点（全 sha が同一で部分一致でも通る）を突いていたため、テストとミューテーションを強化した

## 収束判定
| bot | 依頼回数 | 新規スレッド | 状態 |
|---|---|---|---|
| Codex | 2 回（07:53Z の nudge・12:35Z の `@codex review`） | 3 | 未収束（G3 で再確認） |
| Copilot | 2 回（07:47Z・12:35Z） | 0（インライン）+ 本文 1 | 未収束（G3 で再確認） |
