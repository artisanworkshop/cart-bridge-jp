# R1 レビュー結果（gate-record.sh、PR 前の独立レビュー）
- 対象: `.claude/skills/cbj-dev-cycle/scripts/gate-record.sh`・`gate-record-parse.pl`・`test-gate-record.sh`・fixtures・`quality.sh`・CI・SKILL.md（issue #67）
- レビュアー: フレッシュなコンテキストの独立サブエージェント（`general-purpose`、opus。読み取り専用。CLAUDE.md・`.claude/rules/skill-scripts.md` を読ませたうえで敵対的にレビュー）
- 判定: **CHANGES REQUESTED → すべて対応済み**（High 1・Medium 3・Low 4）。実装者側の裏取り: 各指摘は該当コードを読んで妥当と判断した（修正前に各指摘の再現そのものは実行していない）。修正後に、指摘ごとの回帰テストを足し、ガードを壊すミューテーションで検出できることを確認して固定した

## 指摘と対応
| ID | 重大度 | 場所 | 内容 | 判定・対応 |
|---|---|---|---|---|
| R1-1 | High | `gate-record-parse.pl`（承認の判定） | 「承認」の部分一致のため、`未承認・承認待ち`・`承認なし` や HTML コメント内の `<!-- 承認 -->` でも Resolve のコマンドが出る。「承認済みのときだけ Resolve」という唯一の安全保証がフェイルオープン | **修正**。固定の印 `【承認済み】` を、HTML コメント除去後の判定から肯定一致で読む。値はすべてコメント除去後の文字列から取る。テスト: 否定形 2 種・隠しコメント・肯定形の 4 ケース |
| R1-2 | Medium | `gate-record-parse.pl`（見出しの検出） | コードフェンス内の `# ...`・`## ...` を見出しとして扱い、以降の指摘が黙って消える（`check` が rc=0 のまま検査されない指摘が残る） | **修正**。フェンス（```` ``` ````/`~~~`）を追跡し、中は見出し・ラベルとして解釈しない。閉じていないコメント／フェンスは `unterminated` としてエラー。テスト: フェンス入りの記録で後続の指摘の不正が検出されること・閉じていないコメント／フェンス |
| R1-3 | Medium | `gate-record.sh`（`replies`） | 同じ dbid を複数の指摘に書いても検出せず、別の指摘のスレッドへ返信するコマンドが出る。記録に無い未解決スレッドの通知も無い | **修正**。thread の重複をエラーにし、`replies` は記録に無い未解決 bot スレッドを stderr に通知する（過去ラウンドの保留分でも出るため、エラーにはしない） |
| R1-4 | Medium | `gate-record-parse.pl`（判定の分類） | 前方一致のため `修正不要` が fixed になり Resolve のコマンドが出る | **修正**。判定語の直後に漢字・かな・長音が続く場合は別の語として拒否（`修正済み` は受理）。テスト: `修正不要` の拒否・`修正済み` の受理 |
| R1-5 | Low | `gate-record.sh`（返信ファイル名） | ID をサニタイズした後のファイル名が衝突する（`G9_3` と `G9 3`） | **修正**。ID の文字種を `[A-Za-z0-9._-]` に制限（違反は見出しの崩れとしてエラー） |
| R1-6 | Low | `gate-record-parse.pl`（TODO 検出） | 閉じていない `<!--` が以降の `TODO(記入)` の検出を回避する | **修正**（R1-2 の `unterminated` で対応） |
| R1-7 | Low | `gate-record.sh`（sha の確認） | sha の存在確認がローカルリポジトリだけで、push 済み・PR に含まれるかは見ない | **仕様として明記**（ヘッダ・SKILL.md）。push 済みの確認はネットワークと PR の head が要るため、この PR では入れない |
| R1-8 | Low | `test-gate-record.sh` | `gh api`（レビュー一覧）・`gh pr view --json url,headRefOid` の失敗経路、`^{commit}` を外すミューテーション（tree の sha が通る）、否定形の承認、コードフェンスのテストが無い | **修正**。gh のスタブに `STUB_RC_GH_META`/`STUB_RC_GH_API` を追加し各失敗経路をテスト、tree の sha のケース、承認・フェンスのテストを追加 |

問題なしと確認された点: 出力コマンドの `%q` 引用、返信ファイル名の `tr` によるパストラバーサル防止、ブランチ名 `..` の拒否、`init` の tmp 経由の `mv` と失敗時にファイルを残さないこと、原文コメントの `-->` 無害化、`$VAR` の直後の全角文字が無いこと、bash 3.2 での動作。

## 修正後の検証
- `test-gate-record.sh`: 181 項目すべて ok（レビュー前は 122 項目）。`test-gate-bodies.sh`・`test-gate-turn.sh` も all ok。`bash -n` / `shellcheck -S warning`（全スクリプト）問題なし
- ミューテーション 38 種（承認の部分一致・隠しコメント・判定語の境界・フェンス追跡・重複 dbid・`unterminated`・ID の文字種・tree の sha・`gh api` 失敗の握りつぶしほか）をすべて明示的な FAIL で検出。各実行後にファイルが元どおり復元されたことを確認
- 最初のミューテーション実行で「コメント行を値に混ぜる」だけが未検出だった。値をコメント除去後から取る設計にしたためガードが冗長になり、差が「複数行の値の途中の空行」だけになっていた。その差を固定するテストを足した（テストの穴）
- Ubuntu 24.04（bash 5.2・jq 1.7・perl 5.38）の Docker コンテナで 3 つの dev-tooling テストがすべて通ることを確認（レビュアーが「未検証」とした点）
- 実データ確認: PR #66・#61 に対する `init`（GitHub 読み取りのみ、出力は一時領域）、過去の実記録（`65-gate-turn-script/G2.md`・`G3.md`）に対する `check`

## 対象外指摘・backlog
- なし（R1-7 は仕様として明記）
