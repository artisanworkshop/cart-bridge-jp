# ゲートラウンド G3
- PR: #68 / 対象 HEAD: d75469f
- レビュー: [Codex #5318305329](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#pullrequestreview-5318305329)（対象 d75469f） / [Copilot #5318314602](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#pullrequestreview-5318314602)（🟡 Changes recommended、対象 d75469f）
- 再依頼: 本ラウンドの依頼は Codex（`@codex review`、13:30:51Z、依頼 3 回目）と Copilot（レビュアー指名、13:30:49Z、タイムラインで登録を確認、依頼 3 回目）。**両 bot とも 3 回に達したため、これ以降は再依頼しない**（本ラウンドの修正は push して CI を待つ）。応答は Codex 13:35Z（インライン 3 件）、Copilot 13:36Z（インライン 1 件、判定 🟡 Changes recommended、`Findings: 1 [High]`、本文の `Previously missed` に 3 件）

## 指摘
### [G3-1][Codex P1][.claude/skills/cbj-dev-cycle/scripts/gate-record-parse.pl:77]
要旨: 生成された指摘の見出しを `###` から `####`（や `##`、`###[`）に変えると、見出しとして認識されず、以降のラベルが直前の指摘を黙って上書きする（`check` は成功し、直前の指摘とそのスレッド参照が消える）。
<!-- 判定は 修正 / 保留 / 対応不要 のいずれかで始める。対応不要のスレッドを Resolve するにはユーザー承認を得たうえで判定に 【承認済み】 と書き添える -->
判定: 修正。実コードで再現を確認: `#### [G9-2]` にした 6 件のフィクスチャが `check` を通り（1 件がすり替わる）、`##`・`###[` も rc 0 だった。
対応: 指摘の見出しに見える行（`#` が 1〜6 個 + `[…][` の形）で、正確な `### [` の形でないものを「見出しの崩れ」としてエラーにした（`####`・`##`・`###[`）。テスト 3 ケース、ミューテーション 1 種を検出。
コミット: b2cf44e
スレッド: [r4105040126](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#discussion_r4105040126)
<!-- 原文: **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub> Reject altered finding heading levels** When an editor accidentally changes a generated finding heading from `###` to `####`, this condition does not classify it as malformed; the following labels instead overwrit -->

### [G3-2][Codex P2][.claude/skills/cbj-dev-cycle/scripts/gate-record.sh:129]
要旨: `## 検証` の節（または唯一のプレースホルダ行）を消すと検証欄が空になるが、検証は空を見ないため `check` が通り、テスト・lint・ミューテーションの結果が無い記録をサマリとして公開できる。
<!-- 判定は 修正 / 保留 / 対応不要 のいずれかで始める。対応不要のスレッドを Resolve するにはユーザー承認を得たうえで判定に 【承認済み】 と書き添える -->
判定: 修正。
対応: 判定が修正の指摘が 1 件でもあるのに検証欄が空ならエラーにした（`検証 needs at least one line …`）。指摘が 0 件、または保留・対応不要だけのラウンドはコードを変えないので、検証欄なしでも通す（過剰に厳しくしない）。テスト 2 ケース（欠落で失敗・0 件のラウンドは通る）、ミューテーション 1 種を検出。
コミット: b2cf44e
スレッド: [r4105040132](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#discussion_r4105040132)
<!-- 原文: **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub> Require verification evidence before accepting a record** If an editor deletes the generated `## 検証` section or its only placeholder line, none of these validations reject the now-empty `.verify` array. `check` th -->

### [G3-3][Codex P2][.claude/skills/cbj-dev-cycle/scripts/gate-record.sh:193]
要旨: 1 つの指摘が複数のスレッド（`discussion_r<dbid>` を複数）を持つとき、`replies` は dbid ごとに返信・Resolve を出すのに、サマリの「Resolve したスレッド」などの件数は指摘を 1 件と数えるため過少になる。
<!-- 判定は 修正 / 保留 / 対応不要 のいずれかで始める。対応不要のスレッドを Resolve するにはユーザー承認を得たうえで判定に 【承認済み】 と書き添える -->
判定: 修正（Copilot の本文指摘 G3-B2 と同一）。実コードで確認: 件数は `.findings` の長さで数えていた。
対応: 件数を、該当する指摘の `dbids` の合計（スレッド数）で数えるようにした。テスト（2 スレッドを持つ指摘）、ミューテーション 2 種（Resolve・未解決）を検出。
コミット: b2cf44e
スレッド: [r4105040140](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#discussion_r4105040140)
<!-- 原文: **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub> Count thread IDs instead of findings** When one deduplicated finding references multiple discussion links, `replies` emits a reply and Resolve decision for every dbid, but these expressions count the containing fi -->

### [G3-4][Copilot High][.claude/skills/cbj-dev-cycle/scripts/gate-record.sh:82]
要旨: `--file` は記録の同一性を一切確認しないため、`summary 66 9 --file=<PR #64 の記録>` は PR #64 のヘッダのままのサマリを作り、`replies` は渡した PR 番号（66）で `gate-reply.sh` のコマンドを作る。別 PR に返信するコマンドや、ラベルの違うサマリを出せてしまう。
<!-- 判定は 修正 / 保留 / 対応不要 のいずれかで始める。対応不要のスレッドを Resolve するにはユーザー承認を得たうえで判定に 【承認済み】 と書き添える -->
判定: 修正。実コードで再現を確認: 記録が PR #66 でも引数を 67 にすると、修正前は rc 0 で返信コマンドを出した。
対応: 記録のヘッダ `- PR: #<n>` とタイトル `# ゲートラウンド G<n>` を読み、引数の PR 番号・ラウンドと違えばエラー（両方の番号を表示）、ヘッダ/タイトルが無い記録もエラーにした（`init` の骨組みには必ずある）。`check`/`summary`/`replies` のすべてで効く。Copilot の代案「`--file` を検証専用にする」は取らず、同一性の検証で足りると判断した。テスト 8 項目、ミューテーション 6 種を検出。
コミット: b2cf44e
スレッド: [r4105047827](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#discussion_r4105047827)
<!-- 原文: `--file` bypasses all identity checks, so `summary 66 9 --file=G9-from-pr-64.md` will emit a PR-64 header while `replies` uses the supplied PR 66 when constructing `gate-reply.sh` commands. A wrong file/PR or round argument can therefore publish replies to a different PR and mislabel the summary. Va -->

### [G3-B1][Copilot Medium][.claude/skills/cbj-dev-cycle/scripts/gate-record-parse.pl:57]
要旨: フェンスの検出が「3 つのバッククォートで始まる行」で単純に開閉を反転するため、4 つのバッククォートのフェンスの中にある 3 つのバッククォートの例を閉じフェンスと誤認し、フェンス内容を見出し・ラベルとして解釈してしまう（Copilot 本文の `Previously missed`。スレッド無し）。
判定: 修正。実コードで再現を確認: 4 つのフェンスの中に `### [G9-X]…` を置いた記録が、指摘 G9-X を含むものとして解釈された。
対応: 開きフェンスの文字（バッククォート／チルダ）と長さを覚え、同じ文字で、同じかそれより長く、後ろに空白以外が無い区切りでだけ閉じるようにした。テスト（4 つのバッククォートの中の 3 つ、バッククォートの中のチルダ）、ミューテーション 2 種（文字・長さを見ない）を検出。
コミット: b2cf44e
スレッド: なし（本文指摘。[review 5318314602](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#pullrequestreview-5318314602)）

### [G3-B2][Copilot Medium][.claude/skills/cbj-dev-cycle/scripts/gate-record.sh:194]
要旨: サマリの合計が「スレッド」の件数と言いながら指摘の件数を数えている（`dbids` が複数の指摘は 1 件と数える）。Codex のスレッド G3-3 と同一の指摘（Copilot 本文の `Previously missed`。スレッド無し）。
判定: 修正（G3-3 と同一）。
対応: G3-3 と同じ修正（該当する指摘の `dbids` の合計で数える）。
コミット: b2cf44e
スレッド: なし（本文指摘。[review 5318314602](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#pullrequestreview-5318314602)）

### [G3-B3][Copilot Medium][.claude/skills/cbj-dev-cycle/scripts/gate-record.sh:323]
要旨: 生成する記録が GraphQL の review-thread ID（`.id`）を保持せず、最初のコメントの dbid と URL しか書かない。issue の契約は `init` が各スレッドの ID を記録するとしており、`replies` が未解決スレッドを引き直して復元するため、`init` と `replies` の間に Resolve された・変化したスレッドを記録だけでは表せない、という指摘（Copilot 本文の `Previously missed`。スレッド無し）。
判定: 対応不要（設計判断）【承認済み】。欠陥ではなく意図した設計（ユーザーが「対応不要（設計判断）として記録」を承認）。
対応: `init` が記録するのは安定した公開の識別子（dbid と URL）で、issue の「スレッドごとの ID」は指摘の ID（G<n>-<k>）と dbid を指す。GraphQL の node id は `replies` の時点で dbid から引き直す設計にしてあり、これは意図的: `init` の後に Resolve された・削除されたスレッドを、記録に古い node id が残っていても無警告に操作せず、「未解決の bot スレッドに見つからない」として非ゼロで報告できる。node id を記録に持たせると、Resolve 済みのスレッドへ古い ID で返信・Resolve を出せてしまう。コードの変更なし。
コミット: —
スレッド: なし（本文指摘。[review 5318314602](https://github.com/artisanworkshop/cart-bridge-jp/pull/68#pullrequestreview-5318314602)）

## 検証
- `test-gate-record.sh` 279 項目（235 → 279）すべて ok。`shellcheck -S warning`（全スクリプト）問題なし。過去ラウンドの実記録（G1・G2）も新しい検証（PR 番号・ラウンドの一致、検証欄）で `check` が通る
- **失敗するテストを先に書いて再現を確認してから直した**（G3-1・G3-2・G3-3・G3-4・B1 の 5 件が、修正前のコードで期待どおり失敗した）
- ミューテーション 69 種（G3 で新規 12 種: 階層違いのマーカー・検証欄・PR 番号/ラウンドの一致 4 種・パーサーの読み取り 2 種・スレッド件数 2 種・フェンスの文字と長さ 2 種）をすべて明示的な FAIL で検出し、ファイルが元どおり復元されたことを確認。最初の実行で「スレッド件数を指摘の件数で数える（未解決）」だけが未検出だった。複数スレッドを持つ指摘が「修正」の場合しか検証しておらず、未解決側（保留）の集計が見られていないテストの穴だったので、保留の指摘に 2 スレッドを持たせるテストを追加して検出を確認した
- Ubuntu 24.04（bash 5.2・jq 1.7・perl 5.38）の Docker コンテナで 3 つの dev-tooling テストが通ることを確認

## 収束判定
| bot | 依頼回数 | 新規スレッド | 状態 |
|---|---|---|---|
| Codex | 3 回（07:53Z の nudge・12:35Z・13:30Z の `@codex review`）。上限に達した | 3 | 最終ラウンド。修正は push して CI を待つが再依頼はしない |
| Copilot | 3 回（07:47Z・12:35Z・13:30Z）。上限に達した | 1（インライン）+ 本文 3 | 最終ラウンド。修正は push して CI を待つが再依頼はしない |
