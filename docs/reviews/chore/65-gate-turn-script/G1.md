# ゲートラウンド G1（`/fix-copilot-review` による対応）
- PR: #66 / 対象 HEAD: ea23842
- Codex: 新規 2 件（P2、インライン）/ Copilot: 新規 1 件（インライン、`Findings: 1 [High]`、判定 🟡 Changes recommended）＋総評文の本文指摘 2 件
- 依頼は各ボット 1 回（PR 作成時の自動レビュー）

## 指摘
### [G1-1][Codex P2 + Copilot High（重複）][.claude/skills/cbj-dev-cycle/SKILL.md:83]
案内した `timeout 600000` では、`ci-wait.sh`（最大 600 秒）と `bot-wait.sh`（既定 900 秒）の直列実行が打ち切られ、指摘の取得もタイムアウトの報告も行われない（Copilot は 1,500,000 ms 以上を提案）。
判定: **修正**。SKILL.md 自身が「最長 25 分ほど」と書きながら 10 分を案内していた。
**対応:** 外側の timeout は `(--ci-timeout + --timeout + 60) × 1000` ms 以上と明記（既定 1560000）。`--ci-timeout=秒`（既定 600）を追加して `ci-wait.sh` へ渡し、起動時に必要な値を `budget:` 行で出力。ツールの上限に収まらないときの分割の手順も記載。テスト: `--ci-timeout` の伝達・見積りの出力（既定 / `--no-ci-wait` / オプション反映）。
スレッド: [r4094197211](https://github.com/artisanworkshop/cart-bridge-jp/pull/66#discussion_r4094197211)（Codex）/ [r4094208321](https://github.com/artisanworkshop/cart-bridge-jp/pull/66#discussion_r4094208321)（Copilot）

### [G1-2][Codex P2][.claude/skills/cbj-dev-cycle/scripts/gate-turn.sh:98]
`--first` で 300 秒後の nudge が `@codex review` を投稿し、その後 2 回以上ポーリングすると、`tail -n 4` が「投稿した（Codex への依頼 1 回目として数える）」の行を落とす。一時ファイルは終了時に消えるため、呼び出し側は依頼を消費した唯一の記録を失い、3 回の上限を数え違える。
判定: **修正**。
**対応:** `tail` をやめ、連続する同一行（毎回の進捗）だけ `uniq` で畳んで全出力を残す。nudge の投稿を検出したら最終行に「`@codex review` was posted by the nudge (counts as Codex request #1)」と明記。テスト: nudge を挟んで 2 回以上ポーリングするケースで、記録が残ること・進捗行が畳まれること・nudge が無いときは注記が出ないことを検証。
スレッド: [r4094197224](https://github.com/artisanworkshop/cart-bridge-jp/pull/66#discussion_r4094197224)

### [G1-3][Copilot 総評（本文指摘 B1）][—]
総評文: 「the `--first` ordering contract lacks regression coverage」。`--first` の T を CI 待ちより前に取る（CI 待ちの間に届いた自動レビューを取りこぼさない）という契約が未テスト。
判定: **修正**（PR #64 の G6 と同型: 約束していることがテストされていない）。
**対応:** ci-wait スタブを 2 秒待たせ、bot-wait に渡された T が CI 待ちの完了時刻より前であることをアサート。

### [G1-4][Copilot 総評（本文指摘 B2）][—]
総評文: 「the unresolved timeout and CI/API error-handling issues require correction」。具体的な箇所の記載なし。timeout は G1-1 で対応。
判定: **修正（具体的に特定できた範囲）**。実コードを見直し、`mktemp -d` が失敗すると `TMP` が空のまま `$TMP/ci.out` が `/ci.out`（ルート直下）になる潜在バグを発見。
**対応:** `mktemp` の失敗を検出して exit 3。テスト: `mktemp` を PATH 上のスタブで失敗させ、何も呼ばずに 3 で終わることを検証（macOS の `mktemp -d` は `TMPDIR` を見ないため、環境変数では再現できない）。ほかに具体的な問題は見つからなかった（`ci-wait` の終了コードの写像・API 失敗の伝播は既存テストで検証済み）。

## 検証
- `test-gate-turn.sh`: 59 項目すべて ok（旧 46 項目 + 13）。`bash -n` / `shellcheck -S warning`（全スクリプト）問題なし
- ミューテーション 6 種（`tail` への戻し・nudge 注記の削除・`--first` の T を CI 待ちの後に取る・`mktemp` ガードの削除・`--ci-timeout` の未伝達・見積りから CI 待ちを除外）をすべて明示的な FAIL で検出。元に戻したことを `cmp` で確認
- 途中で、検証用メッセージの `$ci_end）`（変数の直後の全角括弧）が `unbound variable` で FAIL を出さずに異常終了する不具合を自分で再び作り込み、ミューテーションで発見して修正した（`.claude/rules/skill-scripts.md` に追記済みの落とし穴）
