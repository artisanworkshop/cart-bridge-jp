# ゲートラウンド G1
- 対象 HEAD: 6c17ef6（依頼時刻 2026-09-10T16:14:36Z）
- Copilot: 新規 3 件 / Codex: 新規 10 件

## 指摘
### [G1-1][Copilot][Medium][includes/Woo/Tools/SampleCleanup.php:141] 予算枯渇後も取得済みページの残りを処理する
`$remaining` は外側の while でしか見ていないため、先頭の商品のバリエーション削除で予算を使い切っても同じページの残りの商品を削除し続ける。/ 判定: **修正**（G1-5 と同一）
**対応:** ページ内ループの先頭で `$remaining <= 0` なら break。テスト `test_budget_exhausted_by_variations_stops_the_page_early`。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981243346

### [G1-2][Copilot][—][includes/Woo/Tools/MappingRebuilder.php:171] `post_status => 'any'` はゴミ箱も返す
/ 判定: **保留（誤検知）**。WP の `'any'` は `exclude_from_search` が真のステータス（`trash` / `auto-draft`）を除外する。tests-cli で商品をゴミ箱に入れて `get_posts(['post_status'=>'any','post__in'=>[$id]])` が 0 件を返すことを実測。CLAUDE.md に蒸留。
**対応:** 理由を返信し未解決のまま残す。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981243401

### [G1-3][Copilot][Medium][includes/Woo/Tools/SampleCleanup.php:105] プレビューが全 mapping を「消える」扱いにする（画像の doomed 判定も同様）
/ 判定: **修正**（G1-12 と同一）
**対応:** `can_delete_entity()`（`run()` と同じ所有権・実在・権限判定）で `delete` / `unlink` をエンティティ別に分けて返し、画像の doomed 判定も「削除される」と判定した商品・タームだけを渡す。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981243433

### [G1-4][Codex][High][includes/Woo/Writer/CustomerWriter.php:149] 作成マーカーがプラットフォームを持たない（P1）
A が作成 → B が email 突合で採用（`_cbjp_platform`=B）→ B のクリーンアップが A 作成のアカウントを削除しうる。backlog R1-X1（v2.0 で顕在化）として先送りしていたが、修正が小さいため対応。/ 判定: **修正**
**対応:** マーカー値を作成プラットフォームIDにし、`can_delete_user()` は一致する場合のみ削除。テスト `test_marker_from_another_platform_prevents_deletion`。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981259756

### [G1-5][Codex][Medium][includes/Woo/Tools/SampleCleanup.php:159] 予算枯渇後もページ内を処理する（P2）
/ 判定: **修正**（G1-1 と同一対応）/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981259768

### [G1-6][Codex][Medium][includes/Admin/RestController.php:1088] クリーンアップ全体をプラットフォーム単位でロックしていない（P2）
バッチ間に別タブで import が開始されると削除と再作成が競合しうる。`start_run()` と共有するロックが必要で、Sync 層の設計変更を伴う。/ 判定: **保留（要判断）**
**対応:** backlog（R1-L3 の再掲。cancel 競合 f1-6/R1-X1 と合わせて別 issue で設計）。理由を返信し未解決のまま残す。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981259777

### [G1-7][Codex][Medium][src/tabs/ToolsTab.tsx:437] mapping が 0 件だとサンプルセットをクリアできない（P2）
/ 判定: **修正**
**対応:** プレビューに `sample_selected` を追加し、削除対象が 0 件でもサンプルセットが残っていれば「Clear sample selection」として実行（最終処理のみ）できるようにした。テスト `test_run_with_nothing_linked_still_clears_the_sample_selection`。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981259788

### [G1-8][Codex][Low][src/tabs/ToolsTab.tsx:317] バッチ上限到達時に cursor を捨てる（P2）
/ 判定: **修正**（軽微）
**対応:** cursor を state に保持し、再クリックで続きから再開。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981259794

### [G1-9][Codex][Medium][includes/Sync/VerificationReport.php:95] 旧ジョブの `remote_amount` 欠損を 0.00 にする（P2）
アップグレード直後に localStorage から復元した過去の run で偽の「Totals differ」が出る。/ 判定: **修正**
**対応:** `totals_json` にキーが無ければ `remote_amount` を null（不明）で返し、UI は金額突合をスキップ。テスト `test_legacy_jobs_without_remote_amount_report_the_platform_total_as_unknown`。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981259805

### [G1-10][Codex][Medium][includes/Support/Money.php:45] int 範囲を超える金額で TypeError（P2）
`(int)` の飽和 → ×100 で float → 戻り値型違反。`Importer` の try の外で呼ぶためジョブ全体が落ちる。/ 判定: **修正**
**対応:** 整数部 16 桁超・int 入力の範囲超えは null（不明）を返す。テスト追加。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981259811

### [G1-11][Codex][High][includes/Woo/Tools/SampleCleanup.php:280] 削除できない顧客を unlink すると無料版の顧客上限を回避できる（P2）
shop_manager が実行すると作成済みアカウントが残ったまま mappings・サンプルセットがリセットされ、次のサンプルで新たにアカウントを作れる（アーキテクチャ原則 7 に抵触）。/ 判定: **修正**
**対応:** 自プラットフォームが作成したアカウントが mapping に残っているのに実行者が `delete_users` を持たない場合、`run()` は `CleanupNotPermittedException`（REST は 403）で実行自体を拒否。プレビューに `requires_delete_users` / `can_delete_users` を追加し UI でボタンを無効化＋案内。採用アカウントしか無い場合は従来どおり unlink で完了できる。テスト 3 件。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981259819

### [G1-12][Codex][Medium][includes/Woo/Tools/SampleCleanup.php:81] プレビューが削除される件数ではなく mapping 行数を出す（P2）
/ 判定: **修正**（G1-3 と同一対応）/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981259828

### [G1-13][Codex][Low][includes/Sync/MappingRepository.php:163] `local_ids()` が全 ID を1配列に載せる（P2）
int 配列のため 10 万件でも数 MB。`LocalEntityLookup` のクエリは 200 件ずつに分割済み。/ 判定: **保留**
**対応:** backlog（R1-L2 と合わせて Pro 向けの最適化候補）。理由を返信し未解決のまま残す。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3981259837

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Copilot | 1 | 3（修正 2 / 保留 1） | 未収束 |
| Codex | 1 | 10（修正 7 / 保留 2 / 軽微修正 1） | 未収束 |
修正後HEAD: d699107693c08fbaf6e09ca2e421a868bb28fcb2
