# ゲートラウンド G2
- 対象 HEAD: 564e599（依頼時刻 2026-09-10T21:19:35Z）
- Copilot: 新規 1 件 / Codex: 新規 4 件（G1 の保留 3 件は対象外）

## 指摘
### [G2-1][Codex][Medium][includes/Woo/Tools/SampleCleanup.php:61] unlink 時に他プラットフォームの作成マーカーまで消す（P2）
A が作成 → B が採用 → B のクリーンアップが A のマーカーを消すと、A のクリーンアップが削除できず A の顧客上限だけリセットされる。/ 判定: **修正**
**対応:** マーカーは自プラットフォームの値のときだけ外す。テスト `test_marker_from_another_platform_prevents_deletion` に検証を追加。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3983653607

### [G2-2][Codex][Medium][includes/Woo/Tools/SampleCleanup.php:97] プレビューが親商品の削除でカスケードする variation を数えない（P2）
variation の mapping が失われていても `run()` は所有 variation を全て消すため、確認画面の件数が実際より少なくなる。/ 判定: **修正**
**対応:** プレビューの variation 件数を「mapping 行 ∪ 削除される商品の所有 variation」で数える。テスト `test_preview_counts_cascaded_variations_even_when_their_mappings_were_lost`。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3983653612

### [G2-3][Codex][Medium][includes/Sync/VerificationReport.php:79] 店舗通貨が JPY でない場合に金額を同じ通貨として突合する（P2）
`OrderWriter` は数値を換算せず保存し `CURRENCY_MISMATCH` 警告を出すだけなので、¥1,000 と $1,000 が一致扱いになる。/ 判定: **修正**
**対応:** `OrderWriter::PLATFORM_CURRENCY`（JPY）を導入し、レポートに `platform_currency` / `currency_mismatch` を追加。UI は不一致時に金額突合を行わず warning を表示し、ASP 側金額は ASP 通貨で書式化。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3983653619

### [G2-4][Codex][Low][src/components/VerificationReport.tsx:99] `missing` の案内が Rebuild links を勧める（P2）
Rebuild は残っている実体から mapping を作り直すだけで消えた実体は戻せない。/ 判定: **修正**（文言）
**対応:** `missing` は「再 import（またはクリーンアップ）」、`fewer` に「リンクを失った場合は Rebuild links」を案内。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3983653623

### [G2-5][Copilot][Low][src/components/VerificationReport.tsx:138] 受注行が無い／金額が不明でも「order totals match」と表示する
/ 判定: **修正**（文言）
**対応:** 金額突合が成立した受注行がある場合のみ「…and the order totals match」、それ以外は中立な success 文言。/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/34#discussion_r3983687875

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Copilot | 2 | 1（修正 1） | 未収束 |
| Codex | 2 | 4（修正 4） | 未収束 |
修正後HEAD: d6ae5403a5eed9e60b2b19b3b58e86d9e5ea82e8
