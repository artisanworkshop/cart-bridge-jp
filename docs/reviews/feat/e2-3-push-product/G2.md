# ゲートラウンド G2
- 対象 HEAD: cfb9e42（依頼時刻 T=2026-09-14T23:47:17Z、Copilot2回目・Codex3回目=最終）
- Copilot: 新規 2件（スレッド）+ 4件（本文Suppressed comments） / Codex: 新規 4件（スレッド）

## 指摘と対応

### [G2-1][Codex][P2][ColorMeAdapter.php:1075] 画像endpoint専用レート制限（10秒5リクエスト）未モデル化
**判定: 保留**（R1-L4と同一指摘、対応方針変更なし）。スレッド: `#discussion_r4010655931`

### [G2-2][Codex][P2][ColorMeAdapter.php:1119] 画像取得200+空本文が失敗として記録されない
**判定: 修正**。`fetch_image_binary()`が空本文もretryable failureとして記録。commit 4dba073。
スレッド: `#discussion_r4010655934`（Resolve済み）

### [G2-3][Codex][P2][WarningCode.php:288] 終端失敗が実行(非dry-run)結果に可視化されない
**判定: 対象外**（`Sync\Importer`と同じ既存設計方針。CLAUDE.md記載、PR #37で意図的に見送った実績あり）。
backlog `e2-3-push-product/G2-terminal-warnings-not-surfaced`。スレッド: `#discussion_r4010655938`

### [G2-4][Codex][P1][ColorMeAdapter.php:1001] バリエーションのセール価格が運ばれない
**判定: 保留**（backlog `e2-3-push-product/G2-variation-sale-price`）。`Woo\Reader\ProductReader`
（本PR差分範囲外）の変更が必要。スレッド: `#discussion_r4010655940`

### [G2-5][Copilot][High][ColorMeAdapter.php:971] 軸の部分指定（Any属性）でバリエーション衝突
**判定: 修正**。`axis_map_from_pairs()`が名前のみ/値のみの部分指定をフェイルクローズ。commit 4dba073。
スレッド: `#discussion_r4010668078`（Resolve済み）

### [G2-6][Copilot][Medium][ColorMeAdapter.php:1119] G2-2と同根
**判定: 修正**（G2-2で解消）。スレッド: `#discussion_r4010668109`（Resolve済み）

## 本文指摘（系統B・Copilot Suppressed comments）

- **[B1][ColorMeAdapter.php:699]** `fetch_product_detail()`が`product`エンベロープは検証するが
  ネストされた`variants`フィールドの欠損は未検証 → **修正**（commit 4dba073）
- **[B2][ProductTransformer.php:116]** `reduced-rate`以外の`tax_class`（zero-rate等）を無警告で
  標準税率扱い → **修正**（`requires_hidden_safeguard()`に統合。commit 4dba073）
- **[B3][Exporter.php:266]** count一致だが個別variant_remote_idが空文字列＋無警告のケース未対応
  → **保留**（G1-L2と同根、既存backlog）
- **[B4][WarningCode.php:392]** R1の「0円商品が恒久公開されうる」ユーザー確認事項がG1で既に
  対応済みになっており記述が古い → **修正**（`docs/review-backlog.md`のR1-out-of-scope-price-fallback
  エントリを「対応済み」へ更新）

## 収束判定

| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Copilot | 2 | 2スレッド + 4本文 | 未収束（保留2件あり）。3回目は依頼しない方針のため次はfix-copilot-review等で対応 |
| Codex | 3（最終） | 4スレッド | 未収束（保留2件あり）。依頼上限（3回）到達のため再依頼しない |

修正後HEAD: 4dba073
