# ゲートラウンド G1
- 対象 HEAD: c5b1d6f（依頼時刻 T=2026-09-14T22:26:47Z、Codex再依頼 T=2026-09-14T23:13:04Z）
- Copilot: 新規 10件（スレッド）+ 8件（本文Suppressed comments）/ Codex: 新規 9件（スレッド）

## 指摘と対応

### [G1-1][Copilot][High][ColorMeAdapter.php:535,544] 非idempotentなPOST create + 重複作成リスク
既にremote_id確定後の失敗でmappingが書けず重複作成されうる。**判定: 保留**（backlog
`e2-3-push-product/G1-duplicate-on-retry`）。`RateLimitExhaustedException`等の契約拡張が必要で
本PRの差分範囲を超える。スレッド: `#discussion_r4010252377`

### [G1-2][Copilot][High][ColorMeAdapter.php:560] PRODUCT_PRICE_INVALID '0'が有効価格扱い
**判定: 修正**。`PRODUCT_PRICE_INVALID`を`indicates_export_blocking()`へ追加。commit 046e226。
スレッド: `#discussion_r4010252411`（Resolve済み）

### [G1-3][Copilot][High][ColorMeAdapter.php:575,576] 追いPUTがhidden安全策を上書き
**判定: 修正**。追いPUTにも価格未解決時のhidden強制を反映。commit 046e226。
スレッド: `#discussion_r4010252441`（Resolve済み）

### [G1-4][Copilot][High][ColorMeAdapter.php:679] VARIATION_AXIS_LIMIT_EXCEEDED切り捨てでバリエーション衝突
**判定: 修正**。`VARIATION_AXIS_LIMIT_EXCEEDED`を`indicates_export_blocking()`へ追加。commit 046e226。
スレッド: `#discussion_r4010252475`（Resolve済み）

### [G1-5][Copilot][Medium][ColorMeClient.php:137] multipartエスケープのバックスラッシュ順序
**判定: 修正**。バックスラッシュを二重引用符より先にエスケープ。commit 046e226。
スレッド: `#discussion_r4010252508`（Resolve済み）

### [G1-6][Copilot][High][ProductTransformer.php:182] G1-2と同根
**判定: 修正**（G1-2で解消）。スレッド: `#discussion_r4010252533`（Resolve済み）

### [G1-7][Copilot][High][ColorMeAdapter.php:776] fetch_product_detailの200応答スキーマ崩壊未検出
**判定: 修正**。`\$failure`へretryable記録するよう修正。commit 046e226。
スレッド: `#discussion_r4010252556`（Resolve済み）

### [G1-8][Copilot][High][ProductTransformer.php:116] tax_status非課税でtax_reduced=falseへ強制
**判定: 修正**。`TAX_STATUS_NOT_TAXABLE`を`indicates_export_blocking()`へ追加。commit 046e226。
スレッド: `#discussion_r4010252581`（Resolve済み）

### [G1-9][Copilot][High][ProductTransformer.php:197] PRICES_INCLUDE_TAX_DISABLED未考慮
**判定: 保留**（backlog `e2-3-push-product/G1-out-of-scope-prices-include-tax`）。一度export
blocking化を試みたが、`woocommerce_prices_include_tax=no`はフレッシュなWP/WC環境でも既定の
状態（実測: `JobManagerExportTest`が2件失敗）であり、無料版の挙動確認自体ができなくなる店舗が
続出するため取り消した。正しい修正は`ProductReader`側の正規化で本PR差分範囲外。
スレッド: `#discussion_r4010252606`

### [G1-10][Copilot][High][Exporter.php:291] variant数不一致でも親checksumがキャッシュされる
**判定: 修正**。契約違反時は親も未解決扱いにする。commit 046e226。
スレッド: `#discussion_r4010252624`（Resolve済み）

### [G1-11][Codex][P1][ColorMeAdapter.php:579] RateLimitExhaustedExceptionで作成済みmappingが失われる
**判定: 保留**（G1-1と同根、backlog `e2-3-push-product/G1-duplicate-on-retry`）。
スレッド: `#discussion_r4010473398`

### [G1-12][Codex][P1][ProductTransformer.php:218] Woo価格入力モードを考慮しない税換算
**判定: 保留**（G1-9と同根、backlog `e2-3-push-product/G1-out-of-scope-prices-include-tax`）。
スレッド: `#discussion_r4010473406`

### [G1-13][Codex][P2][ProductTransformer.php:229] 税丸め方向の逆転
**判定: 修正**。`php -r`で6000通り検証し、round_down/round_upを逆方向へ修正。commit 046e226。
スレッド: `#discussion_r4010473414`（Resolve済み）

### [G1-14][Codex][P2][ColorMeAdapter.php:776] G1-7と同根
**判定: 修正**（G1-7で解消）。スレッド: `#discussion_r4010473417`（Resolve済み）

### [G1-15][Codex][P2][ProductTransformer.php:120] 更新時にnullフィールドを省略しクリアされない
**判定: 保留**（backlog `e2-3-push-product/G1-update-omits-clear`）。ColorMeのPUT部分更新
セマンティクスが未検証のため、実機確認後に判断。スレッド: `#discussion_r4010473423`

### [G1-16][Codex][P2][ColorMeAdapter.php:594] プラン変更後もchecksum一致で画像push永久スキップ
**判定: 保留**（backlog `e2-3-push-product/G1-capability-change-stale-checksum`）。
スレッド: `#discussion_r4010473430`

### [G1-17][Codex][P2][ProductTransformer.php:113] 商品名100文字上限の未検証
**判定: 保留**（backlog `e2-3-push-product/G1-name-length-validation`）。
スレッド: `#discussion_r4010473436`

### [G1-18][Codex][P1][ProductTransformer.php:77] G1-3と同根
**判定: 修正**（G1-3で解消）。スレッド: `#discussion_r4010473445`（Resolve済み）

### [G1-19][Codex][P2][ColorMeAdapter.php:539] リモート削除済み商品のstale mapping
**判定: 保留**（backlog `e2-3-push-product/G1-stale-mapping-on-404`）。
スレッド: `#discussion_r4010473449`

## 本文指摘（系統B・Copilot Suppressed comments。Resolve不可、ここへの記録が処理済みマーカー）

- **[B1][ColorMeAdapter.php:1072]** 画像取得の404/403を一律retryable扱い → **修正**（`fetch_image_binary()`が
  ステータスで分類。commit 046e226）
- **[B2][ColorMeAdapter.php:538]** G1-19と同根 → **保留**
- **[B3][ColorMeAdapter.php:579]** G1-11と同根 → **保留**
- **[B4][ColorMeAdapter.php:548]** G1-1と同根 → **保留**
- **[B5][ProductTransformer.php:77]** G1-3と同根 → **修正**
- **[B6][ProductTransformer.php:198]** `Cast::to_int_or_null()`の小数切り捨て → **保留**（backlog
  `e2-3-push-product/G1-L1`。JPY運用では実害小）
- **[B7][Exporter.php:299]** count一致だが個別variant_remote_idが空文字列＋無警告のケース →
  **一部対応**（count不一致は修正済み。この「count一致だが個別空・無警告」パターンは未対処、
  backlog `e2-3-push-product/G1-L2`）
- **[B8][WarningCode.php:268]** docblockと実装の食い違い（422の扱い） → **修正**（commit 046e226）

## 収束判定

| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Copilot | 1 | 10スレッド + 8本文 | 未収束（保留分あり。次ラウンドで再依頼） |
| Codex | 2（自動レビュー1回目が15分TIMEOUT→`@codex review`で再依頼） | 9スレッド | 未収束（保留分あり。次ラウンドで再依頼） |

修正後HEAD: 046e226
