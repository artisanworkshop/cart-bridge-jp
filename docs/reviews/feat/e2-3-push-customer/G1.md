# ゲートラウンド G1
- 対象HEAD: 8c3529b（依頼時刻 T=2026-09-15T02:28:50Z）
- Copilot: 新規3件（inline）+ Suppressed 2件 / Codex: 新規4件（inline、うち2件はCopilotの
  Suppressed comments と同一箇所・同旨のため重複排除）

## 指摘

### [G1-1][Copilot][High][includes/Adapters/ColorMe/ColorMeAdapter.php:1217]
新規作成POSTが2xxで`customer.id`を含まない場合、`RuntimeException`で失敗しmappingが一切
書き込まれないため、次回exportが重複POSTしうる。
判定: 保留（`e2-3-push-product/G1-duplicate-on-retry`と同根。`PlatformAdapter`/`PushResult`の
契約拡張が必要で本PR差分範囲を超える）
**対応:** backlogへ（`e2-3-push-customer/G1-duplicate-on-retry`）。返信済み、未Resolve。

### [G1-2][Copilot][Medium相当・保留][includes/Adapters/ColorMe/Transform/CustomerTransformer.php:78,135]
`name`/`mail`の非空性、`furigana`/`hojin`/`busho`/`birthday`等の書式制約を事前検証していない
（inline指摘 dbid=4011402624 + Suppressed comments）。
判定: 保留（`push_product()`も商品名・説明文の長さ等を同様に事前検証しない設計方針と整合。
ただし`name`のmaxLength=50のみ実害が大きい＝必須項目のため422が永久ループするため、この点は
G1-5として個別に対応した）
**対応:** backlogへ（`e2-3-push-customer/G1-optional-field-validation`）。返信済み、未Resolve。

### [G1-3][Copilot][High][includes/Woo/Support/AddressMapper.php:105]
`pref_id_from_state()`の`$`アンカーが末尾改行にマッチし、`JP13\n`のような破損値を正常な
`JP13`として解釈しうる。
判定: 修正
**対応:** `\z`へ変更。commit（下記）。スレッドResolve済み。

### [G1-4][Codex][High（P1）][includes/Adapters/ColorMe/Transform/CustomerTransformer.php:205]
海外住所（`pref_id=48`）で`state`/`country`がColorMeのどのフィールドにも保持されず、
市区町村・番地だけの不完全な住所として無警告でexportされる。
判定: 修正
**対応:** `address1`の末尾に`state`/`country`をカンマ区切りで付記する`append_region()`を追加
（ColorMeスキームに専用フィールドが無いため）。日本国内・往復顧客は付記しない。commit（下記）。
スレッドResolve済み。

### [G1-5][Codex][Medium（P2）][includes/Adapters/ColorMe/Transform/CustomerTransformer.php:132]
`name`（swagger `maxLength: 50`）を事前検証しておらず、50文字超の顧客名は毎回422で
永久に移行できない。
判定: 修正（新規作成のみ）
**対応:** `to_create_payload()`の必須フィールド判定に`mb_strlen($customer->name) > 50`のチェックを
追加。更新（`to_update_payload()`）側は残存リスクとしてbacklogへ
（`e2-3-push-customer/G1-name-length-on-update`。実害小と判断）。commit（下記）。スレッドResolve済み。

### [G1-6][Codex][Medium（P2）][includes/Woo/Support/AddressMapper.php:109]
`state`が`JPxx`形式に一致すれば`country`の値に関わらず国内住所と判定するため、`country`が
非JPを明示していても古い`state`が残っていると誤って国内（東京都等）と判定してしまう。
判定: 修正
**対応:** `country`が非JPを明示している場合は`state`のJPxxパターンを信用せず、海外(48)を優先する
よう変更。commit（下記）。スレッドResolve済み。

### [G1-7][Codex+Copilot（重複）][Medium（対象外・要判断）][includes/Adapters/ColorMe/ColorMeAdapter.php:1208]
既存mappingが指すColorMe顧客が削除済みの場合、更新PUTが404になってもmappingが削除されず、
以後の全exportが同じ削除済みremote_idへPUTし続ける。
判定: 保留（`e2-3-push-product/G1-stale-mapping-on-404`と同根。404検知→新規作成フォールバックの
アダプタ設計が必要で本PR差分範囲を超える）
**対応:** backlogへ（`e2-3-push-customer/G1-stale-mapping-on-404`）。両方に返信済み、未Resolve。

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Copilot | 1 | 5件（3 inline + 2 suppressed） | 未収束（G1-1, G1-2, G1-7を保留のため次ラウンドへ） |
| Codex | 1 | 4件 | 未収束（G1-7を保留のため次ラウンドへ） |

修正後HEAD: （次のcommitで確定）
