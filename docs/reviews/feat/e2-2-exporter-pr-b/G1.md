# ゲートラウンド G1
- 対象 HEAD: 70bb373 → Codex分は 741269b/84068c0（依頼時刻 2026-09-14T06:02:54Z）
- Copilot: 新規 11件（inline 7件 + 本文 Suppressed comments 4件）/ Codex: 新規 15件
  （依頼回数1回目。自動レビューが`70bb373`＝Copilot修正前のstale commitに対して1回、
  `@codex review`再依頼への応答が`b2fb4d9`＝Copilot修正後の最新commitに対して1回、
  計2件のレビュー提出があったが依頼としては1回のためG1に統合）

## 指摘

### [G1-1][Copilot][High][includes/Woo/Reader/CustomerReader.php:29]
`role => 'customer'`は他ロール併存を否定しないため、`shop_manager`等を併せ持つスタッフ
アカウントのPIIがexportされうる。
**対応:** `CustomerWriter::PROTECTED_ROLES`を`public`化し`role__not_in`で除外。テスト追加。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002622790

### [G1-2][Copilot][High][includes/Woo/Reader/OrderReader.php:196]
注文の通貨が`CanonicalOrder`へ一切運ばれず、E2-3の`push_order()`が非JPY注文を誤ってJPYとして
送信しうる。
**対応:** `extras['currency']`を追加し、`OrderWriter::PLATFORM_CURRENCY`と異なる場合
`CURRENCY_MISMATCH`警告を積む。テスト追加。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002622838

### [G1-3][Copilot][High][includes/Woo/Reader/OrderReader.php:257]
`tax_reduced`が`reduced-rate`以外全てを`false`に潰し、`zero-rate`/非課税/送料のみ課税を
見分けられない。
**対応:** 新規`ORDER_LINE_TAX_CLASS_UNSUPPORTED`警告を追加（`ProductReader`の
`TAX_STATUS_NOT_TAXABLE`と同じ非blocking方針）。テスト追加。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002622859

### [G1-4][Copilot][High（指摘は誤り・実測で反証）][includes/Woo/Reader/CouponReader.php:90]
「クーポン金額が符号/上限検証されずexport blockingにならない」という指摘。
**対応:** 実測（`wp eval`）の結果、`WC_Coupon::set_amount()`が投げる`WC_Data_Exception`は
`WC_Data::set_props()`（`read()`が呼ぶ）がプロパティ毎にcatchするため、直接postmetaを
壊しても`new WC_Coupon($id)`で読み直した時点で`get_amount()`は安全な既定値`'0'`を返す
ことを確認。`CouponReader`側の追加検証は到達不能コードになるため実装せず、この実測結果を
固定化するピン留めテストを追加した（CLAUDE.md「指摘自体が誤りである可能性をまず疑う」の実例）。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002622888

### [G1-5][Copilot][High][includes/Woo/Reader/OrderReader.php:238]
数量0以下がそのままexportされうる。
**対応:** `OrderItemBuilder`（インポート方向）と同じ基準で1個へ正規化し
`ORDER_LINE_QUANTITY_INVALID`警告を積む。テスト追加。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002622917

### [G1-6][Copilot][High][includes/Woo/Reader/OrderReader.php:322]
`get_product()`が`false`かどうかは削除済みvariationの信頼できる実在確認にならない
（CLAUDE.md既知の`wc_get_product()`挙動）。
**対応:** 参照先ID（variation優先）に対し`get_post()`で実在確認するよう変更。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002622932

### [G1-7][Copilot][Medium（指摘は過大・実測で範囲を特定）][tests/unit/Woo/Reader/CouponReaderTest.php:27]
`create_coupon()`の2回目`save()`でdraftに落ちる罠が全ての`*_marks_unsupported`系テストに
影響するという指摘。
**対応:** 実測の結果、`WC_Coupon_Data_Store_CPT::update()`が`post_status`を書き直すのは
`code`/`description`/`date_created`/`date_modified`/`status`のいずれかが変更された場合のみで、
現行テストの2回目save（`set_product_ids()`等）はいずれも該当せず実際には壊れていないことを
確認。将来のテスト追加への予防として`create_coupon()`の1回目save前に`set_status('publish')`を
明示。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002622959

### [G1-8][Copilot本文/Suppressed][Medium][includes/Woo/Reader/OrderReader.php:247]
明細金額の単価計算がfloat除算/加算（CLAUDE.md: 金額計算はfloat除算を避け整数演算で行うこと）。
**対応:** `Support\Money`（1/100単位のint）ベースの整数演算に書き換え、round-half-upを
`intdiv()`のみで実装。wp-env実機で1000÷3等の非整数除算の丸めも確認済み。

### [G1-9][Copilot本文/Suppressed][Medium][includes/Woo/Reader/StockReader.php:155]
`instanceof`だけでは削除済みvariationを弾けない（G1-6と同じ理由）。
**対応:** `items_for_variable()`のループ先頭で`get_post()`による実在確認を追加。

### [G1-10][Copilot本文/Suppressed][Medium][includes/Woo/Support/StockDerivation.php:34]
負の在庫数量がそのままexportされうる（`StockApplier::apply()`の書込方向`max(0,...)`と非対称）。
**対応:** `for_product()`に`max(0, ...)`を追加。

### [G1-11][Copilot本文/Suppressed][Medium][includes/Woo/Support/StockDerivation.php:67]
G1-10と同じ、`for_variation()`側。
**対応:** `for_variation()`にも`max(0, ...)`を追加。

## 対応後の品質チェック（Copilot分）
- `composer lint` / `composer analyze`: green
- `composer test:wpenv`: 838 tests, 2288 assertions, green
- wp-env実機（devサイト）で通貨JPY設定・整数除算の丸め・G1-4の実測検証を実施

---

## Codex分（commit_id確認済み: レビュー1件目=`70bb3738`＝Copilot修正前、2件目=`b2fb4d9`＝Copilot修正後）

### [G1-C1][Codex][P1][includes/Woo/Reader/OrderReader.php:554]
店舗通貨が対応ASPの前提（JPY）と異なる注文が無変換でpushされうる。
**対応:** `WarningCode::indicates_export_blocking()`に`CURRENCY_MISMATCH`を追加。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002704251

### [G1-C2][Codex][P2][includes/Woo/Reader/OrderReader.php:217]
`shipping`/`customer_snapshot`の氏名がWestern order（名 姓）で再構成され、ColorMe由来の
日本語順（姓 名）を反転させてしまう。
**対応:** `full_name()`ヘルパーで「姓 名」順に組み直す。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002704254

### [G1-C3][Codex][P1][includes/Woo/Reader/StockReader.php:133]
`manage_stock=true`かつ数量が正でも`stock_status`が明示的に在庫切れの場合、数量から
`in_stock=true`を復元してしまう。
**対応:** `StockDerivation`に`is_in_stock()`チェックを追加。実測（`woocommerce_notify_no_stock_amount`
設定）で再現条件を確認。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002704258

### [G1-C4][Codex][P2（stale・既に対応済み）][includes/Woo/Reader/OrderReader.php:null]
数量0以下がそのままexportされうるという指摘。commit `70bb3738`（Copilot G1修正前）に対する
レビューで、実際には同じ指摘がCopilotのG1-5として既に対応済み。
**対応:** 追加修正なし。stale review である旨を返信。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002704263

### [G1-C5][Codex][P1][includes/Woo/Reader/CouponReader.php:106]
既に一部利用済みのクーポンが未使用の元の上限のままpushされうる。
**対応:** `has_native_restrictions()`に`0 !== $coupon->get_usage_count()`チェックを追加。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002704266

### [G1-C6][Codex][P1][includes/Woo/Reader/OrderReader.php:371]
バリエーション明細の`remote_product_id`にvariant自身のremote_idを使っていたが、ColorMeの
`POST /v1/sales`は`details[].product_id`に親商品を要求する契約。
**対応:** `remote_product_id()`を親商品ID解決に一本化し、`Woo\Support\VariationAxisResolver`
（`ProductReader`から切り出し）でoption1/2値を導出、`option1_value_current`/
`option2_value_current`として明細に追加。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002704267

### [G1-C7][Codex][P1（backlog送り）][includes/Woo/Reader/OrderReader.php:207]
複数配送先注文の`_cbjp_sale_deliveries`が読み戻されず、`push_order()`実装時に残りの配送先を
復元できない。
**対応:** `docs/review-backlog.md`（e2-2-exporter-pr-b/G1codex-M-sale-deliveries）へ送付。
理由: アーキテクチャ原則1に照らした設計判断が必要、かつE2-3の`push_order()`が未実装で
消費経路が無い。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002704275（未解決のまま）

### [G1-C8][Codex][P2（backlog送り）][includes/Woo/Reader/CustomerReader.php:76]
ColorMeインポート時に保存された顧客extras（fax/tel_mobile/sex等）が読み戻されない。
**対応:** `docs/review-backlog.md`（e2-2-exporter-pr-b/G1codex-M-customer-extras）へ送付。
理由はG1-C7と同じ。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002704280（未解決のまま）

### [G1-C9][Codex][P2（backlog送り）][includes/Woo/Reader/CouponReader.php:57]
offsetベースのページングは同時実行下でクーポンの重複/欠落を招きうる。
**対応:** `docs/review-backlog.md`（e2-2-exporter-pr-b/G1codex-M-coupon-cursor-stability）へ
送付。既存の`e2-2-exporter-core/G2-M-pro-tier-pagination`と同基準で保留。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002704286（未解決のまま）

### [G1-C10][Codex][P2][includes/Woo/Reader/OrderReader.php:266]
小数量拡張下で`get_quantity()`がfloatを返すと、`strict_types=1`下の
`divide_minor_units_rounded(int, int)`が`TypeError`でページ全体を落としうる。
**対応:** float経由の数値比較で非整数を検出し丸めたうえで`ORDER_LINE_QUANTITY_INVALID`を積む。
実測: WC本体は既定で`woocommerce_stock_amount`に`intval`フィルターを登録しており
（`wc-core-functions.php`）、この既定を外す拡張が有効な場合にのみ顕在化する。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002781070

### [G1-C11][Codex][P1][includes/Woo/Reader/OrderReader.php:377]
削除済み商品/バリエーションを参照する明細が無警告で`fully_resolved=true`のまま解決済み扱いされる。
**対応:** 新規`ORDER_LINE_PRODUCT_DELETED`（`indicates_export_blocking()`対象）を追加。
**技術的発見:** `get_post()`ベースの存在確認では検出不能と実測判明。
`WC_Order_Item_Product::set_product_id()`は投稿タイプ検証を持ち、削除済み参照は
`WC_Data_Exception`→`set_props()`のプロパティ毎catchにより`get_product_id()`自体が
既定値`0`にリセットされる（`WC_Coupon::set_amount()`と同じパターン）。生のorder-item-meta
（`_product_id`）を`get_metadata()`で直接読んで区別する実装にした。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002781076

### [G1-C12][Codex][P1][includes/Woo/Reader/OrderReader.php:167]
一部/全額返金済みの注文が返金前の金額のままpushされうる。
**対応:** 新規`ORDER_REFUNDED`（`indicates_export_blocking()`対象）を追加。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002781082

### [G1-C13][Codex][P2][includes/Woo/Reader/OrderReader.php:182]
ネイティブなWoo受注の`customer_note`が`_cbjp_memo`のみを見ることで失われる。
**対応:** `note()`ヘルパーで`_cbjp_memo`→`get_customer_note()`の順にフォールバック。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002781089

### [G1-C14][Codex][P1][includes/Woo/Reader/CouponReader.php:101]
非JPY店舗で`fixed_cart`クーポンの金額・最低購入金額が無変換でpushされうる。
**対応:** `OrderReader`と同じ`CURRENCY_MISMATCH`をCouponReaderでも積むようにした
（`percent`型でも`minimum_amount`設定時は対象）。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002781098

### [G1-C15][Codex][P2（指摘は誤り・実測で反証）][includes/Woo/Reader/CouponReader.php:102]
壊れた`date_expires`メタが`null`（無期限）として解釈されうるという指摘。
**対応:** 実測（`wp eval-file`）の結果、`WC_Data::set_date_prop()`は`set_amount()`と異なる
自前のtry/catchを持ち、解釈不能な文字列を`null`ではなくUNIXエポック（既に期限切れ）へ
解決することを確認。修正せず、ピン留めテストを追加。
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/41#discussion_r4002781101

## 対応後の品質チェック（Codex分）
- `composer lint` / `composer analyze`: green
- `composer test:wpenv`: 849 tests, 2309 assertions, green
- `npm run lint`: green
- wp-env実機（`wp eval-file`）で在庫しきい値・削除済み商品参照・小数量フィルター・
  クーポン期限破損の実測検証を実施

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Copilot | 1 | 11（High 5・Medium 6。うち2件は実測で誤り/過大と判明） | 全件対応・Resolve済み（inline 7件）または本文記録（Suppressed 4件） |
| Codex | 1 | 15（P1 6・P2 9。うち2件は実測で誤り/stale） | 修正10件・Resolve済み。誤検知2件・Resolve済み。backlog送り3件・未解決のまま |

修正後HEAD: 84068c0
