# ゲートラウンド G1
- 対象HEAD: 4794735fdc91751b4cd2387cc4ffdbf60b88ee1e（依頼時刻 2026-09-13T10:47:18Z）
- Copilot: 新規2スレッド + 本文6件（Suppressed comments） / Codex: 新規4スレッド

系統A（gate-threads.sh、未解決スレッド）と系統B（gate-bodies.sh、レビュー本文）の両方を取得し、
`path:line`と要旨で重複排除して仕分けた。

## 指摘

### [G1-1][Copilot][スレッド][includes/Woo/Support/MappingCandidates.php:130 / includes/Woo/Writer/OrderWriter.php]
`checkout-draft`の候補一覧からの除外（R1-2/R2-L1）はUI経由の候補一覧のみの防御で、REST直PUTや
除外前に保存済みの`status_map`から紛れ込む経路は防げていなかった。
判定: 修正
**対応:** `MappingCandidates::is_disallowed_order_status()`を新設し、`OrderWriter::apply_status()`
（書込み側）でも同じ値をフェイルクローズするよう変更。回帰テスト
`OrderWriterTest::test_status_mapped_to_checkout_draft_falls_back_to_on_hold_with_warning`を追加。
コミット: 73cd5f5 / スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/39#discussion_r3999421215

### [G1-2][Copilot+Codex][スレッド2件（同一指摘）][includes/Admin/RestController.php:582,583]
`normalized_candidate_list()`のdocblockは「id/nameとも非空文字列化できるスカラー」と書いているが、
実装は空文字列（`''`/`false`のキャスト後）を弾いていなかった。空のASP側idは`SelectControl`の
「未マッピング」placeholderと衝突し、選択してもUNMAPPEDと区別できず`validate_settings_map()`が
保存を拒否する。
判定: 修正
**対応:** id/nameいずれかが空文字列化する要素を除外するよう変更。既存の
`test_get_settings_mappings_filters_out_malformed_adapter_candidates`に空id/空name/false idの
ケースを追加。
コミット: 73cd5f5 / スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/39#discussion_r3999421235 ,
https://github.com/artisanworkshop/cart-bridge-jp/pull/39#discussion_r3999445986

### [G1-3][Codex+Copilot][スレッド+本文（同一指摘）][src/tabs/ExportTab.tsx:285,295]
保存（PUT）が飛んでいる間もマッピングの`SelectControl`は操作可能なままで、保存中に別の行を
変更すると、応答到達時に`setEdited(toEditable(data))`が古いスナップショットで上書きしてしまい、
保存中に行った変更が無言で失われる。
判定: 修正
**対応:** `MappingSection`に`disabled`propを追加し、`saving`中は4テーブル全ての`SelectControl`を
無効化。
コミット: 17285b2 / スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/39#discussion_r3999445983

### [G1-4][Copilot][本文（スレッド無し）][docs/10-tasks.md:270]
E2-1タスクの実装サマリが存在しない`docs/reviews/feat/e2-1-mapping-ui/final-report.md`を参照して
いた（`final-report.md`はdev-cycle Step 8完了時にのみ作成されるため、レビュー時点では未作成）。
判定: 修正
**対応:** 参照先を既にコミット済みの`dev-cycle.md`に変更。
コミット: (このラウンドのdocsコミット参照)

### [G1-5][Copilot][本文（スレッド無し）][docs/reviews/feat/e2-1-mapping-ui/R2.md:21]
「以下のLow 3件のみ検出」と書きつつ実際は4件（R2-L1〜R2-L4）列挙しており、件数表記が実態と
食い違っていた。
判定: 修正
**対応:** 「3件」→「4件」に訂正。
コミット: (このラウンドのdocsコミット参照)

### [G1-6][Copilot][本文（スレッド無し）][src/tabs/ExportTab.tsx:363付近]
`mappingsError`のNoticeを破棄可能にしていたため、破棄すると`mappings`/`edited`がnullのまま
（`platform`が変わらずeffectが再発火しないため）Spinnerだけが永久に残る「詰み」状態になっていた。
判定: 修正
**対応:** `connectionsError`と同様に`isDismissible={false}`にして破棄不可にした。
コミット: 17285b2

## 保留（既にreview-loop R1/R2でLow・backlog記録済み。理由を返信のうえ未解決のまま残す）

### [G1-7][Codex][スレッド][includes/Woo/Support/MappingCandidates.php:50]
カテゴリー候補が親子階層を持たずフラットなため、同名の小カテゴリーが親違いで区別できない。
判定: 保留（`docs/review-backlog.md`の`e2-1-mapping-ui/R1-L2`として既に記録済み）
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/39#discussion_r3999445991

### [G1-8][Codex][スレッド][includes/Adapters/ColorMe/ColorMeAdapter.php:258]
`fetch_categories()`が`hidden`/`members_only`のColorMeカテゴリーを除外するため、エクスポート先の
候補としても選べない。
判定: 保留（`e2-1-mapping-ui/R1-L8`として既に記録済み）
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/39#discussion_r3999445994

### [G1-9][Copilot][本文（スレッド無し）][src/tabs/ExportTab.tsx:190]
`connected`のみでプラットフォームをフィルタしており`needs_reconnect`を区別しない。
判定: 保留（`e2-1-mapping-ui/R1-L6`として既に記録済み。`ImportTab.tsx`の既存パターンを踏襲）

### [G1-10][Copilot][本文（スレッド無し）][src/tabs/ExportTab.tsx:90]
行内`SelectControl`に`label`/`aria-label`が無く、テーブルに`<thead>`/`<th>`も無い。
判定: 保留（`e2-1-mapping-ui/R1-L5`として既に記録済み）

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Copilot | 1 | 2スレッド+6本文 | 未収束（全件対応済みだが次回依頼で収束確認） |
| Codex | 1（自動レビュー不発火のため`@codex review`で明示依頼） | 4スレッド | 未収束（全件対応済みだが次回依頼で収束確認） |

修正後HEAD: 17285b270b1c265a90a4ff3c141c409e2f00fe28（+ この後のdocsコミット）
