# ゲートラウンド G1
- 対象HEAD: f27c580（依頼時刻 T=2026-09-23T06:26:36Z）
- Copilot: 新規1件 / Codex: 新規1件

## 指摘
### [G1-1][Codex][P1相当（重大度High）][src/tabs/ExportTab.tsx:1173]
`RunProgress`のRetryボタンに渡す`retryDisabled`が別run種別（`exportBusy`/`dryRunExportBusy`）のみを
参照しており、同じ種別で新しいrunを開始中（`starting`）の間は旧runの失敗ジョブが引き続き
Retry可能だった。`POST /runs`が解決するまでこのカードは旧runを表示し続けるため、その間に
Retryすると`JobManager::retry()`が`start_run()`の同時実行ガードを経由せずrequeueし、新旧2つの
runが同時に本番へ書き込みうる（重複作成のリスク）。
判定: 修正
**対応:** `cae297f`（Preview export results／Export results両カードの`retryDisabled`に同じ種別の
`starting`を追加）/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/53#discussion_r4079660083

### [G1-2][Copilot][Low（プライバシー）][docs/reviews/feat/e2-4-export-ui-e2e/dev-cycle.md:11]
開発者ローカルの計画ファイルへの絶対パス（ユーザー名を含む）がコミットされていた。
判定: 修正
**対応:** `cae297f`（汎用的な記述に置換）/ スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/53#discussion_r4079669747

## backlog送り（本PRの差分範囲外）
- `e2-4-export-ui-e2e/G1-codex-import-tab-same-gap`: ImportTab.tsxにも同型のRetry競合が既存コードとして存在
- `e2-4-export-ui-e2e/G1-copilot-plan-path-precedent`: 過去にマージ済みの4ファイルにも同型の絶対パス記載が残存

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Codex | 1 | 1件（修正済み） | 未収束（次ラウンドへ） |
| Copilot | 1 | 1件（修正済み） | 未収束（次ラウンドへ） |

修正後HEAD: a8e4da7（push・CI green・スレッド返信/Resolve・サマリコメント投稿済み）
