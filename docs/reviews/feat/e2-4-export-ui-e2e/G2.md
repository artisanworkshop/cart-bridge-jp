# ゲートラウンド G2
- 対象HEAD: e612c2a（依頼時刻 T=2026-09-23T08:32:25Z）
- Copilot: 新規0件（本文指摘1件） / Codex: 新規1件

## 指摘
### [G2-1][Codex][P1相当（重大度High）][src/tabs/ExportTab.tsx:1185]
G1で追加した`retryExportJob`の`starting`ガードは同一タブ内（同一マウントの`ExportTab`）の
競合しか防げない。`JobManager::retry()`自体は`start_run()`が持つ`has_active_job_for_platform()`
相当のチェックを持たずに失敗ジョブをrequeueするため、別タブ/別セッションでの並行実行
（例: 一方のタブでImportを開始中に、別タブでexportの失敗ジョブをRetry）では依然として
同一プラットフォームへの二重書き込みが起こりうる。
判定: 妥当だが根本修正にはバックエンド（`JobManager::retry()`）への変更が必要で、本PR
（フロントエンドのみ・PHP変更なしのスコープ）を超える。人間確認ゲート該当としてユーザーに
提示し、**保留**の判断を得た。
**対応:** `docs/review-backlog.md`の`e2-4-export-ui-e2e/G2-codex-retry-platform-wide-guard`に記録。
スレッドには保留理由を返信し、未解決のまま残す。/ スレッド:
https://github.com/artisanworkshop/cart-bridge-jp/pull/53#discussion_r4080522985

### [G2-2][Copilot（本文指摘）][Low]
`docs/reviews/feat/e2-4-export-ui-e2e/R2.md:21`にG1で修正したはずの実テストショップの
サブドメインの記述が残っていた（別ファイルでの重複記載を見落とし）。
判定: 修正
**対応:** `ec94dd9`（該当ファイルの実サブドメイン表記を除去）。本文指摘のためResolve対象の
スレッドは無し。

## backlog送り
- `e2-4-export-ui-e2e/G2-codex-retry-platform-wide-guard`（High・保留・要判断。ユーザー確認済み）

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Codex | 2 | 1件（保留のまま未解決） | 未収束（次ラウンドへ） |
| Copilot | 2 | 本文1件（修正済み） | 未収束（次ラウンドへ） |

修正後HEAD: ec94dd9（push・CI green・Codexスレッドへ返信〔未Resolve〕・G2サマリコメント投稿済み）
