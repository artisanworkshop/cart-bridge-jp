# ゲートラウンド G1

- 対象 HEAD: `04fe6b0`（依頼時刻 Copilot 2026-09-15T08:55:33Z / Codex `@codex review` 2026-09-15T09:11:29Z、応答 2026-09-15T09:28:13Z）
- Copilot: 新規 2件（1件は本文の`Suppressed comments`、1件はインラインスレッド） / Codex: 新規 6件（すべてインラインスレッド）

## 指摘

### [G1-1][Copilot][High][includes/Adapters/ColorMe/Transform/OrderTransformer.php:239]
明細の`price`は明細合計を数量で割った単価（四捨五入済み）だが、ColorMeの`sale.details[].price`は単価×`product_num`方式のため、合計が数量で割り切れない場合（例: ¥1000÷3個→単価333.33→整数円333、333×3=999）、送信した単価×数量が実際のWoo側合計と一致しなくなる。
判定: 修正（実害検証済み: python3で333×3=999を再現）
**対応:** 合計が数量で割り切れない場合は`price`自体を省略しカタログ価格適用へフォールバック（`unit_price_divides_evenly()`新設）。回帰テスト2件追加。commit `6f9ee6b`
スレッド: https://github.com/artisanworkshop/cart-bridge-jp/pull/45#discussion_r4013919936

### [G1-2][Copilot][Medium（本文のSuppressed comments、スレッドなし）][includes/Adapters/ColorMe/ColorMeAdapter.php:1245]
`push_order()`の早期return（既にエクスポート済み受注のスキップ経路）は`to_create_payload()`を呼ばないため、割引付き受注でも`ORDER_DISCOUNT_NOT_PUSHED`が一切積まれず、`ORDER_UPDATE_NOT_SUPPORTED`だけが報告される。
判定: 修正
**対応:** `OrderTransformer::has_discount()`をI/O不要の`public static`にし、早期returnでも呼べるよう修正。回帰テスト追加。commit `6f9ee6b`
（本文指摘のためスレッドなし。本ファイルとPRコメントが処理済みの記録）

### [G1-3][Codex][P1][includes/Adapters/ColorMe/Transform/OrderTransformer.php:129]
`discount_not_pushed`はpushが実際に走った場合にのみ判明し、`DryRunPlatformWriter`はアダプタを呼ばないためdry-runでは検出できない。
判定: 保留（既知の限界。`e2-3-push-customer/G3-dry-run-validation-parity`と同根で、dry-run経路にアダプタ側検証ロジック相当を呼べるようにする設計拡張が必要。E2-4で扱う）
**対応:** `docs/review-backlog.md` `e2-3-push-order/G1-dry-run-discount-fee-parity`

### [G1-4][Codex][P1][includes/Adapters/ColorMe/Transform/OrderTransformer.php:140]
`payment.fee`/`shipping.fee`（決済手数料・送料）を運ぶフィールドが`sale`のリクエストスキーマに無く、ColorMe側は`payment_id`/`delivery_id`ごとに独自の手数料・送料を適用するため、Woo側の実額と一致しない可能性がある（swagger確認済み: `customer`/`sale_deliveries`/`details`/`payment_id`のみ）。
判定: 修正（`ORDER_DISCOUNT_NOT_PUSHED`と同じ設計で情報提供の警告を追加）
**対応:** `WarningCode::ORDER_FEE_NOT_PUSHED`新設、`OrderTransformer::has_non_representable_charges()`、既存remote_id/skip/成功いずれの経路でも積むよう配線。回帰テスト4件追加。commit（このラウンドで作成）

### [G1-5][Codex][P1][includes/Adapters/ColorMe/Transform/OrderTransformer.php:140]
`CanonicalOrder::$placed_at`（受注日時）を運ぶフィールドがスキーマに無く（swagger確認済み）、ColorMe側は常にpush実行時刻を受注日時とする。日付ベースの売上集計・レポートが実際の購入時期と食い違いうる。
判定: 修正（`PRODUCT_IMAGES_NOT_PUSHED`と同種の、新規作成成功時に常に付く情報提供警告として追加）
**対応:** `WarningCode::ORDER_PLACED_AT_NOT_PRESERVED`新設、`push_order()`の成功パスへ常時付与。回帰テスト追加。commit（このラウンドで作成）

### [G1-6][Codex][P1][includes/Adapters/ColorMe/ColorMeAdapter.php:1274]
`POST /sales`がColorMe側で成功したがレスポンス喪失・タイムアウト・`sale.id`欠落応答の場合、remote_idを返せず`Exporter`はmappingを書き込まないため、次回exportで同じ受注が重複作成されうる。
判定: 保留（対象外・要判断。`e2-3-push-product/customer`の`G1-duplicate-on-retry`と同根の限界で、`PlatformAdapter`/`PushResult`の部分成功remote_id伝搬という契約拡張が必要。本PRの差分範囲を超える）
**対応:** `docs/review-backlog.md` `e2-3-push-order/G1-duplicate-on-retry`

### [G1-7][Codex][P2][includes/Adapters/ColorMe/ColorMeAdapter.php:1245]
既存mappingが指すColorMe側受注が手動削除等で消えていても、`push_order()`は実在確認せず`ORDER_UPDATE_NOT_SUPPORTED`でスキップし続け、当該Woo受注は永久にexportされない。
判定: 保留（対象外・要判断。`e2-3-push-product/customer`の`stale-mapping-on-404`と同根だが、push_orderは更新PUT自体を試みない設計のため404検知の足がかりが無く、専用の実在確認リクエストを追加する設計が必要で対応方針が異なる）
**対応:** `docs/review-backlog.md` `e2-3-push-order/G1-stale-mapping-on-404`

### [G1-8][Codex][P2][includes/Adapters/ColorMe/Transform/OrderTransformer.php:139]
`mapping_candidates()`が返す決済候補（`GET /payments.json`由来）は`type`を保持せず、`POST /v1/sales`のswagger descriptionが受理する決済種別（代金引換・銀行振込・郵便振替・Square・イプシロン・楽天ペイV2・その他決済）に含まれない種別（例: Amazon Pay）もマッピング候補へ含めてしまう。該当種別をマッピングすると受注が毎回422で恒久失敗する。
判定: 保留（要調査。create-sale対応種別がswagger構造化enumではなく文章記載のため対応表を別途作成する調査が必要で、本PRの範囲を超える）
**対応:** `docs/review-backlog.md` `e2-3-push-order/G1-payment-type-filtering`

## 収束判定
| bot | 依頼回数 | 新規指摘 | 状態 |
|---|---|---|---|
| Copilot | 1 | 2件（High 1・Medium 1、いずれも修正） | 次ラウンド待ち |
| Codex | 1（`@codex review`で明示依頼） | 6件（P1×3修正・保留1、P2×2保留） | 次ラウンド待ち |

修正後HEAD: `8e2c928`
