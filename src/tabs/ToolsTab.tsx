import { __ } from '@wordpress/i18n';

/**
 * サンプルクリーンアップ / リンク再構築の実装は F1-7（Phase 1）で行う。
 */
export default function ToolsTab() {
	return <p>{ __( 'Tools are not available yet.', 'cart-bridge-jp' ) }</p>;
}
